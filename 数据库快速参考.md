# 🗄️ 数据库快速参考

## 正式数据库名称
```
jia_shi_yi_xuan
```

## 快速命令

### 连接数据库
```bash
mysql -uroot -p123456 jia_shi_yi_xuan
```

### 查看所有表
```bash
mysql -uroot -p123456 jia_shi_yi_xuan -e "SHOW TABLES;"
```

### 查看表结构
```bash
mysql -uroot -p123456 jia_shi_yi_xuan -e "DESCRIBE t_order;"
```

### 执行 SQL 脚本
```bash
mysql -uroot -p123456 jia_shi_yi_xuan < your_script.sql
```

### 查看数据库大小
```bash
mysql -uroot -p123456 -e "
SELECT
  table_schema AS '数据库',
  ROUND(SUM(data_length + index_length) / 1024 / 1024, 2) AS '大小(MB)'
FROM information_schema.tables
WHERE table_schema = 'jia_shi_yi_xuan'
GROUP BY table_schema;"
```

## ⚠️ 常见错误

### ❌ 错误：使用 `jaseatschoice`
```bash
mysql -uroot -p123456 jaseatschoice  # ❌ 错误数据库
```

### ✅ 正确：使用 `jia_shi_yi_xuan`
```bash
mysql -uroot -p123456 jia_shi_yi_xuan  # ✅ 正确数据库
```

## Spring Boot 配置

**文件：** `JasEatsChoiceJava/src/main/resources/application.yml`

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jia_shi_yi_xuan?...
```

## 数据库备份

### 备份
```bash
mysqldump -uroot -p123456 jia_shi_yi_xuan > backup_$(date +%Y%m%d).sql
```

### 恢复
```bash
mysql -uroot -p123456 jia_shi_yi_xuan < backup_20260113.sql
```

---

**统一使用 `jia_shi_yi_xuan`** 🎯
