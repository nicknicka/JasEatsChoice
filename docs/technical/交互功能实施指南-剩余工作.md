# UniApp交互功能实施指南 - 剩余工作

## 当前状态
- ✅ **DishListCard.vue** 已完成（购物车、收藏API对接）
- ⏳ **FavoriteListCard.vue** 进行中（取消收藏功能）
- ⏳ **OrderListCard.vue** 待开始（订单详情跳转）
- ⏳ **UserInfoCard.vue** 待开始（个人中心跳转）

---

## 剩余工作清单

### 1. FavoriteListCard.vue - 取消收藏功能

**文件路径**：`/src/pages/ai/components/cards/FavoriteListCard.vue`

**步骤1**：已添加API导入 ✅
```javascript
import { favoriteApi } from '@/api/modules/favorite';
```

**步骤2**：修改`handleRemove`函数（第135-154行）

**原代码**：
```javascript
const handleRemove = (item) => {
    console.log('取消收藏:', item);
    emit('action', {
        type: 'remove',
        item: item
    });

    uni.showModal({
        title: '确认取消',
        content: '确定要取消收藏吗？',
        success: (res) => {
            if (res.confirm) {
                uni.showToast({
                    title: '已取消收藏',
                    icon: 'success'
                });
            }
        }
    });
};
```

**新代码**：
```javascript
const handleRemove = async (item) => {
    console.log('取消收藏:', item);
    emit('action', {
        type: 'remove',
        item: item
    });

    // 获取用户ID
    const userId = uni.getStorageSync('userId') || uni.getStorageSync('userInfo')?.userId;
    if (!userId) {
        uni.showToast({
            title: '请先登录',
            icon: 'none'
        });
        return;
    }

    uni.showModal({
        title: '确认取消',
        content: '确定要取消收藏吗？',
        success: async (res) => {
            if (res.confirm) {
                try {
                    // 调用取消收藏API
                    const dishId = item.dishId || item.id;
                    await favoriteApi.removeDish(dishId, userId);

                    uni.showToast({
                        title: '已取消收藏',
                        icon: 'success'
                    });

                    // 触发列表更新事件
                    uni.$emit('favoriteUpdated');
                } catch (error) {
                    console.error('取消收藏失败:', error);
                    uni.showToast({
                        title: error.message || '取消失败，请稍后重试',
                        icon: 'none',
                        duration: 2000
                    });
                }
            }
        }
    });
};
```

**关键修改点**：
1. 函数声明改为`async`
2. 添加用户ID检查
3. 在确认回调中添加`async`关键字
4. 调用`favoriteApi.removeDish(dishId, userId)`
5. 添加try-catch错误处理
6. 触发`favoriteUpdated`事件

---

### 2. OrderListCard.vue - 订单详情跳转

**文件路径**：`/src/pages/ai/components/cards/OrderListCard.vue`

**需要修改**：`handleViewDetail`函数

**查找函数**：
```bash
grep -n "handleViewDetail" /path/to/OrderListCard.vue
```

**预期修改**：
```javascript
const handleViewDetail = (order) => {
    console.log('查看订单详情:', order);
    emit('action', {
        type: 'view_detail',
        order: order
    });

    // 跳转到订单详情页（根据实际路由调整）
    uni.navigateTo({
        url: `/pages-user/order/detail?orderId=${order.orderId}`
    });
};
```

**注意事项**：
- 确认订单详情页的实际路径
- 确认参数名称（orderId还是orderNo）
- 添加emit事件上报

---

### 3. UserInfoCard.vue - 个人中心跳转

**文件路径**：`/src/pages/ai/components/cards/UserInfoCard.vue`

**需要修改**：两个函数

#### 3.1 handleViewProfile函数
```javascript
const handleViewProfile = () => {
    console.log('查看完整资料');
    emit('action', {
        type: 'view_profile',
        data: props.data
    });

    // 跳转到个人中心（根据实际路由调整）
    uni.navigateTo({
        url: '/pages-user/user-center/index'
    });
};
```

#### 3.2 handleEditProfile函数
```javascript
const handleEditProfile = () => {
    console.log('编辑资料');
    emit('action', {
        type: 'edit_profile',
        data: props.data
    });

    // 跳转到编辑页面（根据实际路由调整）
    uni.navigateTo({
        url: '/pages-user/user-center/edit'
    });
};
```

---

## 快速执行脚本

### 方案A：Python脚本（推荐）

创建文件`fix_interactions.py`：

```python
#!/usr/bin/env python3
import os

BASE_PATH = "/Users/nickxiao/JasEatsChoice/JasEatsChoiceUniApp/src/pages/ai/components/cards"

# 1. 修复 FavoriteListCard
def fix_favorite_list():
    file_path = os.path.join(BASE_PATH, "FavoriteListCard.vue")
    with open(file_path, 'r', encoding='utf-8') as f:
        content = f.read()

    # 替换handleRemove函数
    old_func = '''const handleRemove = (item) => {
\t\tconsole.log('取消收藏:', item);
\t\temit('action', {
\t\t\ttype: 'remove',
\t\t\titem: item
\t\t});

\t\tuni.showModal({
\t\t\ttitle: '确认取消',
\t\t\tcontent: '确定要取消收藏吗？',
\t\t\tsuccess: (res) => {
\t\t\t\tif (res.confirm) {
\t\t\t\t\tuni.showToast({
\t\t\t\t\t\ttitle: '已取消收藏',
\t\t\t\t\t\ticon: 'success'
\t\t\t\t\t});
\t\t\t\t}
\t\t\t}
\t\t});
\t};'''

    new_func = '''const handleRemove = async (item) => {
\t\tconsole.log('取消收藏:', item);
\t\temit('action', {
\t\t\ttype: 'remove',
\t\t\titem: item
\t\t});

\t\t// 获取用户ID
\t\tconst userId = uni.getStorageSync('userId') || uni.getStorageSync('userInfo')?.userId;
\t\tif (!userId) {
\t\t\tuni.showToast({
\t\t\t\ttitle: '请先登录',
\t\t\t\ticon: 'none'
\t\t\t});
\t\t\treturn;
\t\t}

\t\tuni.showModal({
\t\t\ttitle: '确认取消',
\t\t\tcontent: '确定要取消收藏吗？',
\t\t\tsuccess: async (res) => {
\t\t\t\tif (res.confirm) {
\t\t\t\t\ttry {
\t\t\t\t\t\t// 调用取消收藏API
\t\t\t\t\t\tconst dishId = item.dishId || item.id;
\t\t\t\t\t\tawait favoriteApi.removeDish(dishId, userId);

\t\t\t\t\t\tuni.showToast({
\t\t\t\t\t\t\ttitle: '已取消收藏',
\t\t\t\t\t\t\ticon: 'success'
\t\t\t\t\t\t});

\t\t\t\t\t\t// 触发列表更新事件
\t\t\t\t\t\tuni.$emit('favoriteUpdated');
\t\t\t\t\t} catch (error) {
\t\t\t\t\t\tconsole.error('取消收藏失败:', error);
\t\t\t\t\t\tuni.showToast({
\t\t\t\t\t\t\ttitle: error.message || '取消失败，请稍后重试',
\t\t\t\t\t\t\ticon: 'none',
\t\t\t\t\t\t\tduration: 2000
\t\t\t\t\t\t});
\t\t\t\t\t}
\t\t\t\t}
\t\t\t}
\t\t});
\t};'''

    if old_func in content:
        content = content.replace(old_func, new_func)
        with open(file_path, 'w', encoding='utf-8') as f:
            f.write(content)
        print("✅ FavoriteListCard.vue 修改完成")
    else:
        print("⚠️ FavoriteListCard.vue 未找到匹配的函数")

if __name__ == '__main__':
    fix_favorite_list()
```

执行：
```bash
python3 fix_interactions.py
```

---

### 方案B：手动修改

1. 打开IDE，定位到文件
2. 查找`handleRemove`函数
3. 复制上面的新代码替换
4. 保存文件
5. 重复其他文件

---

## 验证测试

### 测试步骤

#### 测试1：取消收藏
1. 在AI聊天中发送"查看我的收藏"
2. 点击任意收藏项的"取消收藏"按钮
3. 确认弹窗点击"确定"
4. **预期**：
   - 调用API成功
   - 显示"已取消收藏"Toast
   - 列表项被移除（如果实现了实时更新）

#### 测试2：订单详情
1. 在AI聊天中发送"我的订单"
2. 点击订单卡片
3. **预期**：
   - 跳转到订单详情页
   - 订单ID正确传递

#### 测试3：个人中心
1. 在AI聊天中发送"我的个人信息"
2. 点击"查看完整资料"按钮
3. **预期**：
   - 跳转到个人中心页面

---

## 完成标准

每个功能修改完成后，应该满足：

- ✅ API调用正确
- ✅ 错误处理完善
- ✅ 用户提示友好
- ✅ 登录检查到位
- ✅ 事件上报完整
- ✅ 测试通过

---

**最后更新**：2026-04-02
**维护人**：Claude Code
