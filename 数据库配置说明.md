# 数据库配置说明

## 📊 数据库信息

**项目数据库名称：** `jia_shi_yi_xuan`

### 为什么使用这个名称？

- `jia_shi_yi_xuan` 是"佳食宜选"的中文拼音
- 这是项目的正式数据库名称
- 所有配置文件和SQL脚本都应使用此名称

## 🗄️ 数据库连接配置

### Spring Boot 配置

**文件位置：** `JasEatsChoiceJava/src/main/resources/application.yml`

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jia_shi_yi_xuan?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=true&serverTimezone=GMT%2B8
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
```

## 📝 SQL 脚本使用说明

所有 SQL 脚本都应使用以下格式：

```sql
USE jia_shi_yi_xuan;

-- 你的SQL语句
```

### 现有 SQL 脚本文件

| 文件名 | 说明 | 数据库 |
|--------|------|--------|
| `fix_order_table_id_type.sql` | 修复订单表ID字段类型 | ✅ `jia_shi_yi_xuan` |
| `add_payment_password_columns.sql` | 添加支付密码字段 | ✅ `jia_shi_yi_xuan` |

## ⚠️ 重要提示

1. **不要使用 `jaseatschoice` 数据库**
   - 该数据库可能是早期规划或测试用的
   - 实际应用连接的是 `jia_shi_yi_xuan`

2. **执行 SQL 脚本前确认数据库名**
   ```bash
   # 查看当前数据库
   mysql> SELECT DATABASE();

   # 切换到正确的数据库
   mysql> USE jia_shi_yi_xuan;

   # 或者直接在脚本中指定
   # USE jia_shi_yi_xuan;
   ```

3. **验证表结构**
   ```bash
   mysql -uroot -p123456 -e "USE jia_shi_yi_xuan; SHOW TABLES;"
   ```

## 🔍 数据库管理

### 查看所有数据库

```bash
mysql -uroot -p123456 -e "SHOW DATABASES;"
```

### 查看当前数据库中的表

```bash
mysql -uroot -p123456 jia_shi_yi_xuan -e "SHOW TABLES;"
```

### 查看表结构

```bash
mysql -uroot -p123456 jia_shi_yi_xuan -e "DESCRIBE t_order;"
```

## 📦 数据库迁移（可选）

如果需要将数据从 `jaseatschoice` 迁移到 `jia_shi_yi_xuan`：

```sql
-- 导出数据
mysqldump -uroot -p123456 jaseatschoice > backup.sql

-- 导入数据
mysql -uroot -p123456 jia_shi_yi_xuan < backup.sql
```

**注意：**
- 迁移前请备份两个数据库
- 建议在测试环境先验证
- 确认数据完整性后再在生产环境执行

## ✅ 配置检查清单

- [x] Spring Boot 配置文件使用 `jia_shi_yi_xuan`
- [x] 所有 SQL 脚本使用 `jia_shi_yi_xuan`
- [x] 文档说明已更新
- [ ] 开发团队已通知
- [ ] 部署文档已同步

---

**统一使用 `jia_shi_yi_xuan` 数据库，避免混淆！** 🎯
