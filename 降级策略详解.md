# 推荐拒绝功能 - 降级策略详解

## 📚 什么是降级策略？

**降级策略**（Fallback Strategy）是一种容错机制，当系统的主要功能（通常是更好的方案）不可用时，自动切换到备用方案，确保系统继续运行。

在我们的推荐拒绝功能中：
- **主方案**: 将拒绝历史保存到**后端数据库**（可跨设备同步、永久保存）
- **备用方案**: 保存到**浏览器 localStorage**（仅本地、可能被清除）

---

## 🎯 降级策略工作流程

### 1️⃣ 保存拒绝记录（写入）

```
用户点击"不感兴趣"
    ↓
尝试保存到后端数据库
    │
    ├─→ [成功] ✅
    │   │
    │   └─ 清除 localStorage 缓存
    │      (避免数据冗余)
    │
    └─→ [失败] ❌ (网络错误、服务器宕机等)
        │
        └─ 降级到 localStorage
           │
           ├─ 读取现有数据
           ├─ 更新拒绝次数
           └─ 保存回 localStorage
```

**代码实现** ([useRecommendations.js:52-79](JasEatsChoiceFront/src/renderer/src/composables/useRecommendations.js#L52-L79)):
```javascript
const saveRejectionHistory = async (userId, dishId) => {
  try {
    // 🎯 主方案：尝试保存到后端
    await axios.post(`${API_CONFIG.baseURL}${API_CONFIG.recommendReject.add}`, null, {
      params: { userId, dishId }
    })
    // 后端成功后，清除本地缓存（避免数据重复）
    localStorage.removeItem('rejectionHistory')
  } catch (error) {
    // 🛡️ 降级方案：后端失败时保存到 localStorage
    console.error('保存拒绝历史失败:', error)

    let history = []
    const saved = localStorage.getItem('rejectionHistory')
    if (saved) {
      history = JSON.parse(saved)
    }

    // 更新或添加拒绝记录
    const existingIndex = history.findIndex((entry) => entry.dishId === dishId)
    if (existingIndex > -1) {
      history[existingIndex].count += 1
    } else {
      history.push({
        dishId,
        count: 1,
        rejectedAt: new Date().toISOString()
      })
    }

    localStorage.setItem('rejectionHistory', JSON.stringify(history))
  }
}
```

### 2️⃣ 加载拒绝历史（读取）

```
用户打开推荐页面
    ↓
尝试从后端加载拒绝历史
    │
    ├─→ [成功] ✅
    │   │
    │   └─ 使用后端数据
    │      (最新、完整的拒绝历史)
    │
    └─→ [失败] ❌ (网络错误、服务器宕机等)
        │
        └─ 降级到 localStorage
           │
           ├─ 尝试读取 localStorage
           └─ 返回本地数据（可能不完整）
```

**代码实现** ([useRecommendations.js:33-47](JasEatsChoiceFront/src/renderer/src/composables/useRecommendations.js#L33-L47)):
```javascript
const loadRejectionHistory = async () => {
  try {
    // 🎯 主方案：从后端加载
    const userId = String(authStore.userId || 1)
    const response = await axios.get(`${API_CONFIG.baseURL}${API_CONFIG.recommendReject.list}`, {
      params: { userId }
    })
    return response.data.data || []
  } catch (error) {
    // 🛡️ 降级方案：后端失败时读取 localStorage
    console.error('加载拒绝历史失败:', error)
    const saved = localStorage.getItem('rejectionHistory')
    return saved ? JSON.parse(saved) : []
  }
}
```

---

## 🔄 数据同步策略

### 正常情况（后端可用）

```
┌─────────────────────────────────────────────────────────┐
│                    用户操作流程                           │
└─────────────────────────────────────────────────────────┘

用户点击"不感兴趣"
    ↓
保存到后端数据库 ✅
    ↓
清除 localStorage ✅
    ↓
下次加载时从后端读取 ✅
```

**关键点**：一旦后端恢复，localStorage 中的旧数据会被清除，确保以数据库为准。

### 异常情况（后端不可用）

```
┌─────────────────────────────────────────────────────────┐
│                降级模式操作流程                          │
└─────────────────────────────────────────────────────────┘

用户点击"不感兴趣"
    ↓
尝试保存到后端... ❌ 失败
    ↓
保存到 localStorage ✅
    ↓
继续使用本地数据过滤推荐
```

**关键点**：即使后端不可用，用户仍能正常使用推荐功能，不会影响体验。

---

## 📊 方案对比

| 特性 | 后端数据库（主方案） | localStorage（备用方案） |
|------|---------------------|------------------------|
| **持久性** | ✅ 永久保存 | ⚠️ 清除浏览器缓存会丢失 |
| **跨设备同步** | ✅ 支持 | ❌ 仅本机 |
| **数据完整性** | ✅ 完整 | ⚠️ 依赖用户操作 |
| **可靠性** | ✅ 高（事务支持） | ⚠️ 中等 |
| **性能** | ✅ 适合大量数据 | ⚠️ 大量数据影响性能 |
| **网络依赖** | ❌ 需要网络 | ✅ 离线可用 |

---

## 🛡️ 降级策略的优势

### 1. 提高可用性
```
后端服务: ████████░░░░░░░░░ 80% 可用
    ↓
+ 降级策略
    ↓
整体服务: ██████████░░░░░ 95%+ 可用
```

### 2. 无缝用户体验
- 用户感知不到后端故障
- 推荐过滤功能持续可用
- 数据不丢失

### 3. 渐进式恢复
```
后端故障期间
    ↓
数据保存在 localStorage
    ↓
后端恢复后
    ↓
自动切换回数据库
    ↓
数据从 localStorage 迁移到数据库（可选）
```

---

## 🔍 实际应用场景

### 场景1：网络波动
```
用户点击"不感兴趣"
    ↓
网络不稳定，请求超时
    ↓
自动保存到 localStorage ✅
    ↓
用户继续使用，不受影响
    ↓
网络恢复后，下次自动同步到后端
```

### 场景2：后端维护
```
后端服务器维护中
    ↓
所有拒绝记录暂时保存在本地
    ↓
用户正常使用推荐功能
    ↓
后端恢复后，数据可以迁移（见下方迁移脚本）
```

### 场景3：首次使用
```
新用户首次使用
    ↓
可能还没有数据库账户
    ↓
先使用 localStorage 保存
    ↓
登录后自动迁移到后端数据库
```

---

## 📦 数据迁移（从 localStorage 到后端）

当后端恢复可用后，可以使用以下脚本将本地数据迁移到数据库：

```javascript
// 在浏览器开发者工具 Console 中执行
const migrateLocalToBackend = async () => {
  const saved = localStorage.getItem('rejectionHistory')
  if (!saved) {
    console.log('✅ 无数据需要迁移')
    return
  }

  const history = JSON.parse(saved)
  const userId = String(authStore.userId || 1)

  console.log(`📦 开始迁移 ${history.length} 条拒绝记录...`)

  let successCount = 0
  let failCount = 0

  for (const item of history) {
    try {
      await axios.post(`${API_CONFIG.baseURL}${API_CONFIG.recommendReject.add}`, null, {
        params: {
          userId,
          dishId: String(item.dishId),
          reason: '从localStorage迁移'
        }
      })
      successCount++
      console.log(`✅ 已迁移: ${item.dishId}`)
    } catch (error) {
      failCount++
      console.error(`❌ 迁移失败: ${item.dishId}`, error)
    }
  }

  console.log('\n📊 迁移统计:')
  console.log(`总计: ${history.length} 条`)
  console.log(`成功: ${successCount} 条`)
  console.log(`失败: ${failCount} 条`)

  if (successCount > 0 && failCount === 0) {
    // 全部迁移成功，清除本地数据
    localStorage.removeItem('rejectionHistory')
    console.log('✅ 本地数据已清除')
  }
}

// 执行迁移
migrateLocalToBackend()
```

---

## 💡 最佳实践建议

### 1. 监控降级频率
```javascript
// 添加监控日志
const saveRejectionHistory = async (userId, dishId) => {
  try {
    await axios.post(...)
    // 记录主方案使用
    console.log('[降级策略] ✅ 使用后端数据库')
  } catch (error) {
    // 记录降级发生
    console.warn('[降级策略] ⚠️ 降级到 localStorage')

    // 可以发送监控告警
    // monitorAlert('推荐拒绝保存降级', error)
  }
}
```

### 2. 定期数据校验
```javascript
// 定期检查 localStorage 中是否有陈旧数据
const cleanupOldData = () => {
  const saved = localStorage.getItem('rejectionHistory')
  if (saved) {
    const history = JSON.parse(saved)
    const sevenDaysAgo = Date.now() - 7 * 24 * 60 * 60 * 1000

    // 清除7天前的本地数据
    const filtered = history.filter(item => {
      return new Date(item.rejectedAt).getTime() > sevenDaysAgo
    })

    localStorage.setItem('rejectionHistory', JSON.stringify(filtered))
  }
}

// 每次加载时执行
cleanupOldData()
```

### 3. 用户提示
```javascript
// 当降级发生时，友好提示用户
const saveRejectionHistory = async (userId, dishId) => {
  try {
    await axios.post(...)
  } catch (error) {
    // 保存到本地后，给出温和提示
    ElMessage.info({
      message: '网络不稳定，已保存在本地',
      duration: 2000,
      showClose: false
    })
    // ... localStorage 逻辑
  }
}
```

---

## 🎯 总结

### 降级策略核心价值
1. **高可用性** - 即使后端故障，功能仍可用
2. **数据安全** - 多层备份，不丢失用户数据
3. **用户友好** - 对用户透明，无感知切换
4. **易于恢复** - 后端恢复后自动切回

### 实现要点
- ✅ **优先后端** - 正常情况总是使用数据库
- ✅ **自动降级** - 失败时无缝切换到 localStorage
- ✅ **数据清理** - 后端成功后清除本地冗余
- ✅ **错误处理** - 完善的异常捕获和日志

这是一个**生产级的降级策略实现**，确保了推荐拒绝功能的健壮性和可靠性！🎉
