# Recommend.vue 动态导入错误修复指南

## 问题描述

在导航到推荐页面时，出现以下错误：

```
GET http://localhost:5173/src/composables/useFavorites.js 500 (Internal Server Error)
GET http://localhost:5173/src/composables/useRecommendations.js net::ERR_ABORTED 500
TypeError: Failed to fetch dynamically imported module: /src/views/user/Recommend.vue
```

## 根本原因

### ❌ 错误的代码模式

两个 composable 文件都在**模块顶层**调用了 `useAuthStore(pinia)`：

```javascript
// useFavorites.js 和 useRecommendations.js
import pinia from '../../store'
import { useAuthStore } from '../../store/authStore'

const authStore = useAuthStore(pinia)  // ❌ 在模块加载时立即执行

export function useComposable() {
  // ...
}
```

### 为什么会导致错误？

1. **模块加载时机问题**
   - 当 `Recommend.vue` 被动态导入时，所有依赖的模块也会被加载
   - 模块顶层的代码会在导入时**立即执行**
   - 此时 Pinia 实例可能还没有完全初始化

2. **动态导入的特殊性**
   - 动态导入（`import()`）是异步的
   - 模块执行时机与静态导入不同
   - 依赖的 store 可能在此时无法访问

3. **500 错误的含义**
   - Vite 开发服务器返回 500 错误
   - 表示模块解析或执行失败
   - 通常是由于未捕获的 JavaScript 错误

## ✅ 修复方案

### 正确的代码模式

将 `useAuthStore(pinia)` 的调用从**模块顶层**移到**函数内部**：

```javascript
// ✅ 正确：在函数内部获取 store
import pinia from '../../store'
import { useAuthStore } from '../../store/authStore'

export function useComposable() {
  // 在函数调用时才获取 store
  // 此时 Pinia 已经完全初始化
  const authStore = useAuthStore(pinia)

  // ...
}
```

### 为什么这样能解决问题？

1. **延迟初始化**
   - `useAuthStore(pinia)` 只在函数被调用时执行
   - 此时 Vue 应用已经完全启动
   - Pinia store 已经正确初始化

2. **保持灵活性**
   - 每次调用函数时都会获取最新的 store 状态
   - 避免了模块加载时的时序问题

3. **兼容动态导入**
   - 无论静态导入还是动态导入都能正常工作
   - 不依赖模块加载顺序

## 修复内容

### 1. useFavorites.js

**文件**: `/src/renderer/src/composables/useFavorites.js`

**修改前**:
```javascript
import pinia from '../../store'
import { useAuthStore } from '../../store/authStore'

const authStore = useAuthStore(pinia)  // ❌ 模块顶层

export function useFavorites() {
  // ...
}
```

**修改后**:
```javascript
import pinia from '../../store'
import { useAuthStore } from '../../store/authStore'

export function useFavorites() {
  const authStore = useAuthStore(pinia)  // ✅ 函数内部

  // ...
}
```

### 2. useRecommendations.js

**文件**: `/src/renderer/src/composables/useRecommendations.js`

**修改前**:
```javascript
import pinia from '../../store'
import { useAuthStore } from '../../store/authStore'

const authStore = useAuthStore(pinia)  // ❌ 模块顶层

export function useRecommendations() {
  // ...
}
```

**修改后**:
```javascript
import pinia from '../../store'
import { useAuthStore } from '../../store/authStore'

export function useRecommendations() {
  const authStore = useAuthStore(pinia)  // ✅ 函数内部

  // ...
}
```

## 验证修复

1. **刷新页面**
   - 修复后需要刷新浏览器页面
   - 清除可能的缓存

2. **导航到推荐页面**
   - 点击菜单中的"推荐"选项
   - 应该能正常加载推荐页面

3. **检查控制台**
   - 不应该再有 500 错误
   - 不应该有模块导入失败的错误

## 最佳实践

### ✅ 推荐做法

**Composable 中使用 Pinia Store**:

```javascript
import { ref } from 'vue'
import pinia from '@/store'
import { useAuthStore } from '@/store/authStore'

export function useMyComposable() {
  // ✅ 在函数内部获取 store
  const authStore = useAuthStore(pinia)

  // 使用 store
  const userId = authStore.userId

  return { userId }
}
```

### ❌ 避免的做法

```javascript
import pinia from '@/store'
import { useAuthStore } from '@/store/authStore'

// ❌ 不要在模块顶层调用 store
const authStore = useAuthStore(pinia)

export function useMyComposable() {
  // ...
}
```

## 相关概念

### 模块加载时机

**静态导入** (`import`):
```javascript
import { foo } from './foo.js'  // 在模块加载时执行
```

**动态导入** (`import()`):
```javascript
const module = await import('./foo.js')  // 在运行时执行
```

### Pinia Store 初始化

```javascript
// main.js
import { createPinia } from 'pinia'

const pinia = createPinia()
app.use(pinia)  // Pinia 在这里初始化

// 在此之前，useAuthStore(pinia) 可能无法正常工作
```

## 其他可能的问题

如果修复后仍有问题，请检查：

### 1. Pinia 实例导出

**文件**: `/src/renderer/src/store/index.js`

确保正确导出 pinia 实例：

```javascript
import { createPinia } from 'pinia'

const pinia = createPinia()

export default pinia
```

### 2. Store 定义

**文件**: `/src/renderer/src/store/authStore.ts`

确保 store 正确定义：

```javascript
import { defineStore } from 'pinia'

export const useAuthStore = defineStore('auth', {
  state: () => ({
    token: '',
    userId: null
  })
})
```

### 3. 应用初始化顺序

确保 Vue 应用按正确顺序初始化：

```javascript
// 1. 创建 Pinia
const pinia = createPinia()

// 2. 创建 App
const app = createApp(App)

// 3. 使用 Pinia
app.use(pinia)

// 4. 挂载应用
app.mount('#app')
```

## 总结

### 问题根源
- 在模块顶层调用 `useAuthStore(pinia)`
- 动态导入时的模块加载时机问题

### 解决方案
- 将 store 获取移到函数内部
- 确保在 Pinia 完全初始化后才访问 store

### 关键要点
- ✅ Composable 中应该在函数内获取 store
- ✅ 避免模块顶层的副作用代码
- ✅ 确保依赖初始化顺序正确

---

**修复日期**: 2025-01-19
**影响范围**: useFavorites.js, useRecommendations.js
**修复人**: Claude Code Assistant
