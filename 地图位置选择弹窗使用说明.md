# 地图位置选择弹窗使用说明

## 功能概述

新的地图位置选择弹窗组件 `CommonMapLocationPicker.vue` 提供了以下功能：

### 核心功能
1. **地图交互选择** - 点击地图任意位置选择坐标
2. **地址搜索** - 支持搜索关键词查找地址
3. **实时定位** - 获取当前GPS位置
4. **地址反解析** - 自动将坐标转换为详细地址
5. **搜索结果列表** - 显示搜索结果，点击快速定位

## 组件使用

### 基础用法

```vue
<template>
  <CommonMapLocationPicker
    v-model:visible="mapLocationPickerVisible"
    :default-position="{ lng: 116.397428, lat: 39.90923 }"
    @location-selected="handleLocationSelected"
  />
</template>

<script setup>
import { ref } from 'vue'
import CommonMapLocationPicker from '@/components/CommonMapLocationPicker.vue'

const mapLocationPickerVisible = ref(false)

const handleLocationSelected = (locationData) => {
  const { position, address } = locationData
  console.log('选中的位置:', position)
  console.log('详细地址:', address)
}
</script>
```

### Props 参数

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| visible | Boolean | false | 控制弹窗显示/隐藏 |
| defaultPosition | Object | { lng: 116.397428, lat: 39.90923 } | 默认地图中心位置 |

### Events 事件

| 事件名 | 参数 | 说明 |
|--------|------|------|
| update:visible | (visible: Boolean) | 弹窗显示状态变化时触发 |
| location-selected | (locationData: Object) | 用户确认选择位置后触发 |

#### location-selected 事件数据结构

```javascript
{
  position: {
    lng: 116.397428,  // 经度
    lat: 39.90923     // 纬度
  },
  address: "北京市朝阳区望京SOHO"  // 详细地址
}
```

## 功能说明

### 1. 地图交互
- 点击地图任意位置，会在该点添加标记
- 自动进行地址反解析，显示详细地址
- 支持地图缩放和拖动

### 2. 地址搜索
- 在搜索框输入地址关键词
- 点击搜索或按回车键搜索
- 搜索结果会在下方列表显示
- 点击搜索结果，地图自动定位到该位置

### 3. GPS定位
- 点击右下角定位按钮
- 自动获取当前GPS位置
- 地图中心移动到当前位置
- 自动解析当前位置的详细地址

### 4. 确认选择
- 选择位置后，点击"确认选择"按钮
- 触发 `location-selected` 事件
- 返回选中位置的坐标和地址信息

## UI设计特点

### 视觉优化
1. **现代化卡片设计** - 采用圆角卡片、渐变按钮
2. **平滑动画** - 标记添加、按钮悬停都有流畅动画
3. **阴影层次** - 使用box-shadow增加层次感
4. **图标美化** - 使用Element Plus图标库

### 用户体验
1. **加载状态提示** - 地图加载时显示loading动画
2. **定位加载状态** - GPS定位时显示加载动画
3. **空状态处理** - 未选择位置时禁用确认按钮
4. **错误提示** - 使用ElMessage显示操作反馈

### 响应式设计
- 弹窗宽度自适应
- 地图容器固定高度
- 搜索结果列表最大高度限制，超出滚动

## 技术实现

### 集成高德地图SDK
- 版本：高德地图 JavaScript API 2.0
- 加载插件：
  - AMap.Scale - 比例尺
  - AMap.ToolBar - 工具栏
  - AMap.Geocoder - 地理编码
  - AMap.PlaceSearch - 地址搜索

### 关键API调用

#### 1. 地图初始化
```javascript
const map = new AMap.Map('mapContainer', {
  zoom: 15,
  center: [lng, lat],
  mapStyle: 'amap://styles/normal'
})
```

#### 2. 标记添加
```javascript
const marker = new AMap.Marker({
  position: [lng, lat],
  animation: 'AMAP_ANIMATION_DROP'
})
map.add(marker)
```

#### 3. 地址搜索
```javascript
const placeSearch = new AMap.PlaceSearch({
  city: '全国'
})
placeSearch.search(keyword, callback)
```

#### 4. 逆地理编码
```javascript
const geocoder = new AMap.Geocoder()
geocoder.getAddress([lng, lat], callback)
```

## 集成到现有页面

### 在首页集成示例

在 `HomeContent.vue` 中已经集成：

```vue
<template>
  <!-- 位置选择按钮 -->
  <button @click="mapLocationPickerVisible = true">
    点击选择位置
  </button>

  <!-- 地图位置选择弹窗 -->
  <CommonMapLocationPicker
    v-model:visible="mapLocationPickerVisible"
    @location-selected="handleLocationSelected"
  />
</template>

<script setup>
import { ref } from 'vue'
import CommonMapLocationPicker from '@/components/CommonMapLocationPicker.vue'

const mapLocationPickerVisible = ref(false)

const handleLocationSelected = (locationData) => {
  const { address } = locationData
  // 更新天气位置
  weather.value.address = address
  weather.value.city = extractCityFromAddress(address)
  // 获取该城市天气
  fetchWeather(weather.value.city)
}
</script>
```

## 配置说明

### 高德地图API Key
项目已配置高德地图API Key：
- Key: `140e4ebfe143855a4cc7440533ff27b3`
- 配置位置：`src/renderer/index.html`

### CSP安全策略
已更新 Content-Security-Policy 允许加载高德地图资源：
```html
<meta
  http-equiv="Content-Security-Policy"
  content="script-src 'self' 'unsafe-inline' https://webapi.amap.com; ..."
/>
```

## 注意事项

1. **网络要求** - 需要联网才能加载高德地图SDK
2. **API配额** - 注意高德地图API调用次数限制
3. **HTTPS** - 生产环境建议使用HTTPS
4. **定位权限** - GPS定位需要用户授权
5. **地图容器** - 确保地图容器有明确的宽度和高度

## 未来优化方向

1. **路线规划** - 添加起点到终点的路线规划
2. **周边搜索** - 搜索周边的餐厅、商场等
3. **收藏位置** - 支持保存常用位置
4. **历史记录** - 记录用户选择过的位置
5. **多选功能** - 支持一次选择多个位置点
6. **地图样式** - 提供多种地图主题切换

## 问题排查

### 地图不显示
- 检查网络连接
- 确认API Key是否正确
- 查看浏览器控制台错误信息

### 定位失败
- 检查浏览器定位权限
- 确认设备支持GPS
- 尝试在HTTPS环境下使用

### 搜索无结果
- 确认搜索关键词格式
- 检查高德地图API配额
- 尝试更通用的关键词

## 相关文件

- 组件文件：`src/renderer/src/components/CommonMapLocationPicker.vue`
- 首页集成：`src/renderer/src/views/user/HomeContent.vue`
- SDK加载：`src/renderer/index.html`
