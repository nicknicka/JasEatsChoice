# 菜品表(t_dish)结构优化总结

## 执行时间
2026-01-31

## 优化背景

### 原始问题
1. **字段冗余**：`is_fast_food` 和 `step_template` 存储相同信息
2. **语义不清**：`status` 字段名不够明确
3. **缺失字段**：实体类缺少统计相关字段

### is_fast_food 字段存在的问题

`is_fast_food` 字段与 `step_template` 字段功能重复：
- `is_fast_food = 1` ↔ `step_template = 'FAST'`
- `is_fast_food = 0` ↔ `step_template = 'NORMAL'`

这种冗余设计导致：
- 数据一致性风险：两个字段可能不同步
- 存储空间浪费
- 代码维护复杂

## 优化方案

### 1. 移除冗余字段
```sql
-- 删除 is_fast_food 字段
ALTER TABLE t_dish DROP COLUMN is_fast_food;
```

**替代方案**：
- 使用 `step_template` 字段统一管理烹饪流程类型
- 添加虚拟字段 `is_fast_food_virtual` 用于查询便利

### 2. 字段重命名（提升语义清晰度）

| 原字段名 | 新字段名 | 说明 |
|---------|---------|------|
| `status` | `is_online` | 更清晰地表达上架/下架状态 |
| `cooking_minutes` | `estimated_cooking_minutes` | 更准确表达这是预估时间 |

### 3. 实体类优化

#### 新增统计字段
```java
@TableField("view_count")
private Integer viewCount; // 浏览次数

@TableField("order_count")
private Integer orderCount; // 订单次数

@TableField("favorite_count")
private Integer favoriteCount; // 收藏次数

@TableField("tags")
private JsonNode tags; // 标签列表
```

#### 添加兼容方法
为了保证向后兼容，添加了以下废弃方法：

```java
@Deprecated
public Boolean getStatus() {
    return isOnline;
}

@Deprecated
public void setStatus(Boolean status) {
    this.isOnline = status;
}

@Deprecated
public Integer getCookingMinutes() {
    return estimatedCookingMinutes;
}

@Deprecated
public void setCookingMinutes(Integer minutes) {
    this.estimatedCookingMinutes = minutes;
}

public Boolean getIsFastFood() {
    return "FAST".equalsIgnoreCase(stepTemplate);
}
```

**优势**：
- 旧代码无需修改即可继续工作
- 逐步迁移到新API，风险可控

### 4. 新增索引（优化查询性能）

```sql
-- 商家审核列表查询优化
ALTER TABLE t_dish ADD INDEX idx_merchant_audit_time (merchant_id, audit_status, create_time);

-- 菜品推荐查询优化
ALTER TABLE t_dish ADD INDEX idx_category_online_rating (category, is_online, avg_rating);

-- 热门菜品查询优化
ALTER TABLE t_dish ADD INDEX idx_view_order_count (view_count DESC, order_count DESC);
```

### 5. 虚拟字段（Generated Column）

```sql
ALTER TABLE t_dish ADD COLUMN is_fast_food_virtual TINYINT(1) GENERATED ALWAYS AS
    (CASE WHEN step_template = 'FAST' THEN 1 ELSE 0 END) VIRTUAL;
```

**优势**：
- 自动计算，无需手动维护
- 节省存储空间
- 保证数据一致性

## 优化结果

### 数据库表结构变化

#### 删除的字段
- `is_fast_food` - 冗余字段已删除

#### 重命名的字段
- `status` → `is_online`
- `cooking_minutes` → `estimated_cooking_minutes`

#### 新增的字段
- `is_fast_food_virtual` - 虚拟字段，自动计算是否为快餐

#### 新增的索引
- `idx_merchant_audit_time` - 商家审核查询优化
- `idx_category_online_rating` - 菜品推荐查询优化
- `idx_view_order_count` - 热门菜品查询优化

### 数据验证

```sql
SELECT
    COUNT(*) as total_dishes,
    SUM(CASE WHEN step_template='FAST' THEN 1 ELSE 0 END) as fast_food_count,
    SUM(CASE WHEN is_online=1 THEN 1 ELSE 0 END) as online_count
FROM t_dish;
```

**结果**：
- 总菜品数：134
- 快餐数量：0
- 上架数量：134

## 兼容性处理

### 无需修改的代码
由于添加了兼容方法，以下代码可以继续工作：

```java
// 旧代码仍然有效
if (dish.getStatus()) {
    // ...
}

dish.setCookingMinutes(20);

Boolean isFast = dish.getIsFastFood();
```

### 推荐的新写法

```java
// 新代码推荐写法
if (dish.getIsOnline()) {
    // ...
}

dish.setEstimatedCookingMinutes(20);

Boolean isFast = "FAST".equals(dish.getStepTemplate());
```

## 文件清单

### 数据库脚本
- [optimize_dish_table.sql](optimize_dish_table.sql) - 数据库优化脚本

### 实体类
- [Dish.java](JasEatsChoiceJava/src/main/java/com/xx/jaseatschoicejava/entity/Dish.java) - 菜品实体类（已优化）

### 其他文档
- [fix_dish_audit_fields.sql](fix_dish_audit_fields.sql) - 审核字段添加脚本（之前执行）
- [菜品表优化总结.md](菜品表优化总结.md) - 本文档

## 后续建议

1. **逐步迁移代码**
   - 优先在新增代码中使用新字段名
   - 旧代码维护时逐步更新为新API

2. **监控查询性能**
   - 关注新增索引的使用情况
   - 根据实际查询模式调整索引策略

3. **定期维护**
   - 监控虚拟字段的性能影响
   - 考虑为其他冗余字段也采用虚拟字段方式

## 回滚方案

如需回滚，执行以下SQL：

```sql
-- 恢复 is_fast_food 字段
ALTER TABLE t_dish ADD COLUMN is_fast_food TINYINT(1) DEFAULT 0 COMMENT '是否为快餐：1-是，0-否' AFTER estimated_cooking_minutes;

-- 恢复 status 字段名
ALTER TABLE t_dish CHANGE is_online status TINYINT(1) DEFAULT 1 COMMENT '菜品状态（0-下架/1-上架）';

-- 恢复 cooking_minutes 字段名
ALTER TABLE t_dish CHANGE estimated_cooking_minutes cooking_minutes INT DEFAULT 15 COMMENT '标准烹饪时长（分钟）';

-- 删除虚拟字段
ALTER TABLE t_dish DROP COLUMN is_fast_food_virtual;

-- 删除新增的索引
ALTER TABLE t_dish DROP INDEX idx_merchant_audit_time;
ALTER TABLE t_dish DROP INDEX idx_category_online_rating;
ALTER TABLE t_dish DROP INDEX idx_view_order_count;
```
