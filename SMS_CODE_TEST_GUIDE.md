# 短信验证码登录测试指南

## ✅ 后端修改完成

### 已完成的修改：
1. ✅ **LoginRequest.java** - 添加了 `code` 字段（短信验证码）
2. ✅ **UserController.java** - 修改了 `login` 方法，支持短信验证码登录
3. ✅ **编译成功** - BUILD SUCCESS，Lombok 已生成 getter/setter 方法

### 后端登录逻辑：
```java
// 判断登录方式
if (smsCode != null && !smsCode.isEmpty()) {
    // 短信验证码登录
    // 1. 从 Redis 获取存储的验证码
    // 2. 支持测试验证码 123456
    // 3. 验证通过后查询用户
    // 4. 生成 token 并返回用户信息
} else if (password != null && !password.isEmpty()) {
    // 密码登录
    // 1. 验证图形验证码
    // 2. 调用 userService.login()
}
```

## 🚀 重启后端服务

### 方法1：使用 IDE 重启
如果你使用 IntelliJ IDEA 或 Eclipse：
1. 停止当前运行的服务
2. 重新启动 `JasEatsChoiceJavaApplication`

### 方法2：使用命令行
```bash
cd /Users/nickxiao/JasEatsChoice/JasEatsChoiceJava

# 停止旧服务（如果在运行）
# 找到 Java 进程并停止

# 启动服务
./mvnw spring-boot:run
```

### 方法3：使用重新编译的 JAR
```bash
# 打包
./mvnw clean package -DskipTests

# 运行
java -jar target/JasEatsChoiceJava-0.0.1-SNAPSHOT.jar
```

## 📱 测试短信验证码登录

### 步骤1：启动后端服务
确保后端服务正常运行在 `http://localhost:8080`

### 步骤2：启动 UniApp 小程序
打开微信开发者工具，加载 UniApp 项目

### 步骤3：测试登录流程

#### 3.1 输入手机号
```
手机号：13800138000
```

#### 3.2 点击"获取验证码"
- 查看控制台日志：
```
====== 测试验证码 ======
手机号: 13800138000
验证码: 123456
提示: 请使用验证码 123456 进行登录
======================
```

- 查看弹窗提示：
```
【开发模式】请使用验证码: 123456
有效期5分钟
```

#### 3.3 输入验证码
```
验证码：123456
```

#### 3.4 点击登录
- ✅ 登录成功！

## 🔍 验证流程

```
┌─────────────────────────────────────────────────────────────┐
│                      前端（UniApp）                         │
├─────────────────────────────────────────────────────────────┤
│  1. 用户点击"获取验证码"                                     │
│     ↓                                                        │
│  2. 生成固定验证码: 123456                                  │
│     ↓                                                        │
│  3. 存储到本地缓存 (5分钟有效)                               │
│     ↓                                                        │
│  4. 弹窗显示验证码                                            │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                      后端（Spring Boot）                      │
├─────────────────────────────────────────────────────────────┤
│  5. 用户输入验证码并点击登录                                 │
│     ↓                                                        │
│  6. 接收登录请求: POST /v1/users/login                      │
│     {                                                        │
│       "phone": "13800138000",                               │
│       "code": "123456"                                       │
│     }                                                        │
│     ↓                                                        │
│  7. 从 Redis 获取验证码:                                     │
│     key = "sms-code:13800138000"                            │
│     value = "123456" (或随机验证码)                           │
│     ↓                                                        │
│  8. 验证验证码:                                              │
│     - 如果输入 "123456" → 直接通过（测试模式）                  │
│     - 否则比较 Redis 中的验证码                               │
│     ↓                                                        │
│  9. 验证通过后:                                              │
│     - 查询用户信息                                           │
│     - 生成 JWT Token                                        │
│     - 返回用户信息和 Token                                    │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                    前端接收响应                              │
├─────────────────────────────────────────────────────────────┤
│  10. 保存 Token 和用户信息                                    │
│  11. 跳转到首页                                              │
│  12. ✅ 登录成功！                                           │
└─────────────────────────────────────────────────────────────┘
```

## 📊 后端日志

正常登录日志：
```
2026-03-29 11:43:24 INFO  - 使用短信验证码登录，手机号: 13800138000
2026-03-29 11:43:24 INFO  - 使用测试验证码登录
2026-03-29 11:43:24 INFO  - 短信验证码登录成功: 13800138000
```

错误日志（如果出现问题）：
```
验证码不存在或已过期 → Redis 中没有该手机号的验证码
验证码错误 → 输入的验证码与 Redis 中存储的不一致
用户不存在，请先注册 → 该手机号未注册
```

## 🧪 测试场景

### 场景1：使用测试验证码登录（推荐）
```
手机号：13800138000
验证码：123456（固定测试验证码）
结果：✅ 登录成功
```

### 场景2：使用随机验证码登录
```
1. 点击"获取验证码"
2. 后端生成随机验证码（如：654321）
3. 存储到 Redis（5分钟有效）
4. 输入随机验证码：654321
5. ✅ 登录成功
```

### 场景3：验证码过期
```
1. 点击"获取验证码"
2. 等待5分钟
3. 输入验证码：123456
4. ❌ 验证码已过期，请重新获取
```

### 场景4：验证码错误
```
1. 点击"获取验证码"
2. 输入错误验证码：999999
3. ❌ 验证码错误
```

### 场景5：用户不存在
```
1. 输入未注册的手机号：19999999999
2. 点击"获取验证码"并输入验证码
3. ❌ 用户不存在，请先注册
```

## 🛠️ 故障排查

### 问题1：后端无法启动
**检查点**：
- 端口 8080 是否被占用
- MySQL 是否正常运行
- Redis 是否正常运行

**解决方法**：
```bash
# 检查端口占用
lsof -i:8080

# 检查 MySQL
ps aux | grep mysql

# 检查 Redis
redis-cli ping
```

### 问题2：登录时提示"验证码不能为空"
**原因**：后端未重新编译或未重启

**解决方法**：
1. 确保后端编译成功：`./mvnw clean compile`
2. 重启后端服务
3. 刷新前端页面

### 问题3：提示"验证码不存在或已过期"
**原因**：Redis 中没有验证码数据

**解决方法**：
1. 检查 Redis 是否正常：`redis-cli ping`
2. 确认是否调用了发送验证码接口
3. 查看后端日志

### 问题4：前端无法连接后端
**检查点**：
- 后端是否启动：访问 `http://localhost:8080`
- API 配置是否正确：查看 `/src/api/config/index.js`
- 网络请求是否正常：F12 → Network

## 📝 关键代码位置

### 后端：
1. **LoginRequest.java**
   - 路径：`JasEatsChoiceJava/src/main/java/com/xx/jaseatschoicejava/entity/LoginRequest.java`
   - 修改：添加了 `code` 字段

2. **UserController.java**
   - 路径：`JasEatsChoiceJava/src/main/java/com/xx/jaseatschoicejava/controller/UserController.java`
   - 修改：`login` 方法支持短信验证码登录

### 前端：
1. **发送验证码**
   - 路径：`JasEatsChoiceUniApp/src/pages/login/index.vue`
   - 方法：`sendCode()`

2. **登录处理**
   - 路径：`JasEatsChoiceUniApp/src/store/modules/user.js`
   - 方法：`login()`

## ✅ 验证成功的标志

### 前端：
```
✅ 模拟验证码验证通过
✅ 登录成功
跳转到首页
```

### 后端：
```
使用短信验证码登录，手机号: 13800138000
使用测试验证码登录
短信验证码登录成功: 13800138000
```

## 🎉 现在可以开始测试了！

按照以上步骤操作，短信验证码登录功能应该能够正常工作。如果遇到问题，请查看"故障排查"部分。

---

**最后更新**: 2026-03-29 11:43
**编译状态**: ✅ BUILD SUCCESS
**作者**: Claude Code
