# 钱包和支付功能部署说明

## 📦 功能概述

已完成用户钱包和支付功能的完整实现，包括：
- ✅ 钱包余额管理
- ✅ 充值/提现功能
- ✅ 订单支付扣费
- ✅ 交易记录查询
- ✅ 余额校验机制

## 🗄️ 数据库部署

### 步骤1：执行数据库脚本

```bash
# 连接到MySQL数据库
mysql -u root -p jia_shi_yi_xuan

# 执行建表脚本
source /Users/nickxiao/JasEatsChoice/create_wallet_tables.sql
```

### 步骤2：验证表结构

执行完成后，数据库将包含以下新表：

| 表名 | 说明 | 关键字段 |
|------|------|----------|
| `t_wallet` | 用户钱包表 | user_id, balance, version, status |
| `t_payment_record` | 支付记录表 | payment_no, order_id, amount, payment_status |
| `t_recharge_record` | 充值记录表 | recharge_no, amount, recharge_status |
| `t_withdraw_record` | 提现记录表 | withdraw_no, amount, withdraw_status |

### 步骤3：扩展订单表

订单表 `t_order` 已自动添加以下字段：
- `payment_id` - 支付记录ID
- `paid_amount` - 已支付金额
- `payment_time` - 支付时间

## 🔧 后端部署

### 新增文件清单

```
JasEatsChoiceJava/src/main/java/com/xx/jaseatschoicejava/
├── entity/
│   ├── Wallet.java                    # 钱包实体
│   ├── PaymentRecord.java             # 支付记录实体
│   ├── RechargeRecord.java            # 充值记录实体
│   └── WithdrawRecord.java            # 提现记录实体
├── mapper/
│   ├── WalletMapper.java              # 钱包Mapper
│   ├── PaymentRecordMapper.java       # 支付记录Mapper
│   ├── RechargeRecordMapper.java      # 充值记录Mapper
│   └── WithdrawRecordMapper.java      # 提现记录Mapper
├── service/
│   ├── WalletService.java             # 钱包服务接口
│   ├── PaymentService.java            # 支付服务接口
│   └── impl/
│       ├── WalletServiceImpl.java     # 钱包服务实现
│       └── PaymentServiceImpl.java    # 支付服务实现
└── controller/
    └── WalletController.java          # 钱包控制器
```

### 修改文件

- `OrderController.java` - 添加支付接口
- `Order.java` - 添加支付相关字段
- `ConsumeHistoryService.java` - 扩展接口继承IService
- `ConsumeHistoryServiceImpl.java` - 继承ServiceImpl获得save方法

### 启动后端服务

```bash
cd JasEatsChoiceJava
mvn clean package
java -jar target/JasEatsChoiceJava-*.jar
```

### API接口清单

#### 钱包接口 `/v1/wallet`

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/info/{userId}` | 获取钱包信息 |
| GET | `/balance/{userId}` | 获取余额 |
| GET | `/check?userId=&amount=` | 检查余额是否足够 |
| POST | `/recharge` | 充值 |
| POST | `/withdraw` | 提现 |

#### 支付接口 `/v1/orders`

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/{orderId}/pay` | 订单支付 |
| GET | `/{orderId}/payment` | 获取支付记录 |

## 💻 前端部署

### 新增文件

```
JasEatsChoiceFront/src/renderer/src/
├── api/
│   ├── wallet.js                      # 钱包API
│   └── payment.js                     # 支付API
```

### 修改文件

- `views/user/OrderConfirmation.vue` - 实现真实支付逻辑
- `views/user/Profile.vue` - 实现真实充值/提现逻辑

### 启动前端服务

```bash
cd JasEatsChoiceFront
npm install
npm run dev
```

## 🧪 功能测试

### 1. 钱包初始化测试

访问个人中心页面，系统会自动为用户创建钱包（余额为0）。

**验证方法**：
```sql
SELECT * FROM t_wallet WHERE user_id = 1;
```

### 2. 充值功能测试

1. 进入个人中心页面
2. 点击"充值"按钮
3. 输入充值金额（如：100）
4. 确认充值

**预期结果**：
- 显示"充值成功"提示
- 钱包余额增加
- `t_wallet` 表 `balance` 字段更新
- `t_consume_history` 表新增充值记录

### 3. 余额校验测试

1. 进入订单确认页面
2. 确认当前余额
3. 尝试支付超过余额的订单

**预期结果**：
- 显示"余额不足"错误提示
- 支付无法进行

### 4. 订单支付测试

1. 添加商品到购物车
2. 进入订单确认页面
3. 确认余额充足
4. 点击"确认支付"

**预期结果**：
- 支付成功提示
- 余额扣减
- 订单状态更新为"待接单"
- `t_payment_record` 表新增支付记录
- `t_consume_history` 表新增消费记录

### 5. 提现功能测试

1. 进入个人中心页面
2. 点击"提现"按钮
3. 输入提现金额（不超过余额）
4. 确认提现

**预期结果**：
- 显示"提现成功"提示
- 钱包余额减少
- `t_wallet` 表 `balance` 字段更新
- `t_consume_history` 表新增提现记录

## 🔒 安全特性

### 1. 乐观锁机制

钱包表使用 `version` 字段实现乐观锁，防止并发修改导致的数据不一致。

```java
@Version
private Integer version;
```

### 2. 事务保证

所有涉及金额变更的操作都使用 `@Transactional` 注解保证原子性。

```java
@Transactional(rollbackFor = Exception.class)
public boolean deductBalance(Long userId, BigDecimal amount, String description) {
    // 扣费逻辑
}
```

### 3. 余额校验

支付前强制校验余额是否充足。

```java
if (platformBalance.value < finalAmount.value) {
  ElMessage.error(`余额不足！当前余额：¥${platformBalance.value.toFixed(2)}，需要：¥${finalAmount.value.toFixed(2)}`)
  return
}
```

## 📊 数据流转

### 充值流程

```
用户点击充值
  → 前端调用 walletApi.recharge()
    → WalletController.recharge()
      → WalletService.recharge()
        → 更新 t_wallet.balance
        → 插入 t_consume_history 记录
          → 返回成功，前端更新显示
```

### 支付流程

```
用户点击支付
  → 前端校验余额
    → 调用 paymentApi.payOrder()
      → OrderController.payOrder()
        → PaymentService.createPayment()
        → PaymentService.processPayment()
          → WalletService.deductBalance()
            → 更新 t_wallet.balance
            → 插入 t_consume_history 记录
            → 更新 t_order 状态
              → 返回成功，前端跳转到订单列表
```

## ⚠️ 注意事项

1. **数据库备份**：部署前务必备份数据库
2. **API鉴权**：生产环境需要添加JWT Token验证
3. **支付安全**：建议接入微信/支付宝支付接口
4. **并发控制**：高并发场景建议添加Redis分布式锁
5. **日志记录**：重要操作已添加日志，便于问题追踪

## 🐛 故障排查

### 问题1：支付时提示"余额不足"

**原因**：钱包余额确实不足
**解决**：先充值再支付

### 问题2：充值后余额未更新

**原因**：前端缓存未刷新
**解决**：刷新页面或重新进入个人中心

### 问题3：支付失败提示"500"

**原因**：后端异常
**排查**：
1. 检查后端日志
2. 确认数据库表是否创建成功
3. 检查用户ID是否正确

### 问题4：钱包表字段不匹配

**原因**：数据库脚本未执行
**解决**：重新执行 `create_wallet_tables.sql`

## 📈 后续优化建议

1. **支付方式扩展**：集成微信支付、支付宝支付
2. **分布式锁**：使用Redis实现分布式锁，防止并发问题
3. **幂等性保证**：使用支付流水号实现支付幂等
4. **提现审核**：添加提现审核流程
5. **对账机制**：定期对账，确保数据一致性
6. **通知功能**：支付成功后发送通知
7. **退款功能**：实现订单退款流程
8. **钱包密码**：添加钱包支付密码功能

## ✅ 部署检查清单

- [ ] 数据库脚本已执行
- [ ] 后端服务已重启
- [ ] 前端服务已重启
- [ ] 钱包表数据已验证
- [ ] 充值功能测试通过
- [ ] 支付功能测试通过
- [ ] 提现功能测试通过
- [ ] 余额校验测试通过
- [ ] 交易记录正常生成

## 📞 技术支持

如有问题，请检查：
1. 后端日志：`logs/jaseatschoicejava.log`
2. 浏览器控制台：F12 查看前端错误
3. 数据库日志：MySQL错误日志

---

**部署完成后，系统即可正常使用钱包和支付功能！** 🎉
