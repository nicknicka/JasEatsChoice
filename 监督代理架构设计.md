# 监督代理架构设计

## 设计日期
2026-03-25

## 架构模式
**LangChain4j Agentic Supervisor Pattern**

---

## 架构层次

### 📊 三层架构设计

```
┌─────────────────────────────────────────────────┐
│     SupervisorAgent (监督代理 - L3)              │
│  - 智能调度，自主规划执行流程                      │
│  - 决定调用哪些子Agent                            │
│  - 使用AgenticScope共享状态                       │
└─────────────────┬───────────────────────────────┘
                  │
    ┌─────────────┼─────────────┬──────────────┐
    ▼             ▼             ▼              ▼
┌────────┐  ┌──────────┐  ┌────────┐  ┌──────────┐
│ Order  │  │ Nutrition│  │Merchant│  │Recommen- │
│ Agent  │  │  Agent   │  │ Agent  │  │ dation   │
│        │  │          │  │        │  │  Agent   │
└───┬────┘  └────┬─────┘  └────┬───┘  └────┬─────┘
    │            │            │             │
    ▼            ▼            ▼             ▼
┌────────┐  ┌──────────┐  ┌────────┐  ┌──────────┐
│Order   │  │Nutrition │  │Merchant│  │Recommen- │
│Tools   │  │Tools     │  │Tools   │  │dation   │
│        │  │          │  │        │  │Tools     │
└────────┘  └──────────┘  └────────┘  └──────────┘
```

---

## 子Agent定义（L2）

### 1. OrderAgent（订单Agent）
**职责**：处理所有订单相关操作
**工具**：
- `OrderQueryTools` - 查询订单、状态、历史
- `OrderCreateTools` - 创建订单、计算价格
- `OrderGuideTools` - 下单引导

### 2. RecommendationAgent（推荐Agent）
**职责**：提供个性化推荐
**工具**：
- `RecommendationQueryTools` - 查询推荐内容
- `RecommendationFilterTools` - 过滤推荐结果
- `RecommendationRankTools` - 排序推荐结果

### 3. NutritionAgent（营养Agent）
**职责**：营养分析和健康建议
**工具**：
- `NutritionQueryTools` - 查询营养信息
- `NutritionRecordTools` - 记录营养摄入
- `NutritionAnalysisTools` - 分析营养数据

### 4. MerchantAgent（商家Agent）
**职责**：商家信息查询
**工具**：
- `MerchantQueryTools` - 查询商家、菜品、评分

### 5. UserAgent（用户Agent）
**职责**：用户信息管理
**工具**：
- `UserQueryTools` - 查询用户信息
- `UserProfileTools` - 用户偏好设置

---

## 监督代理配置（L3）

### 关键配置项

```java
SupervisorAgent supervisor = AgenticServices
    .supervisorBuilder()
    .chatModel(plannerModel)           // 规划模型
    .subAgents(                         // 子代理列表
        orderAgent,
        recommendationAgent,
        nutritionAgent,
        merchantAgent,
        userAgent
    )
    .responseStrategy(
        SupervisorResponseStrategy.SCORED  // 响应策略
    )
    .contextGenerationStrategy(
        SupervisorContextStrategy.CHAT_MEMORY_AND_SUMMARIZATION
    )
    .supervisorContext("""
        # 系统约束
        - 优先使用用户ID进行查询
        - 数据不足时主动询问用户
        - 提供准确的操作结果
    """)
    .build();
```

---

## AgenticScope 状态共享

### 共享变量设计

| 变量名 | 类型 | 说明 | 写入Agent | 读取Agent |
|--------|------|------|-----------|-----------|
| `userId` | String | 用户ID | 所有 | 所有 |
| `request` | String | 用户请求 | Supervisor | 所有 |
| `orderResult` | String | 订单查询结果 | OrderAgent | Supervisor |
| `recommendations` | List | 推荐列表 | RecommendationAgent | Supervisor |
| `nutritionInfo` | String | 营养信息 | NutritionAgent | Supervisor |
| `merchantInfo` | String | 商家信息 | MerchantAgent | Supervisor |

---

## 与旧架构对比

| 对比项 | 旧架构（AiServices） | 新架构（AgenticServices） |
|--------|---------------------|-------------------------|
| **构建方式** | `AiServices.builder()` | `AgenticServices.supervisorBuilder()` |
| **Agent定义** | `@SystemMessage` | `@Agent(description) + @SystemMessage` |
| **状态共享** | ChatMemory | AgenticScope |
| **调用方式** | `.tools(agent)` | `.subAgents(agent)` |
| **规划能力** | ❌ LLM被动响应 | ✅ LLM自主规划 |
| **输出配置** | 无 | `.outputName("变量名")` |

---

## 实施步骤

### ✅ 第1步：创建Agent接口（带@Agent注解）
- [ ] OrderAgent.java
- [ ] RecommendationAgent.java
- [ ] NutritionAgent.java
- [ ] MerchantAgent.java
- [ ] UserAgent.java

### ✅ 第2步：创建配置类
- [ ] AgenticSupervisorConfig.java

### ✅ 第3步：修改Controller
- [ ] AIStreamController.java - 使用SupervisorAgent

### ✅ 第4步：测试验证
- [ ] 编译测试
- [ ] 功能测试

---

## 优势分析

### 使用监督代理的好处

1. **自主规划能力**
   - LLM可以自主生成执行计划
   - 不需要硬编码调用流程
   - 根据上下文动态调整

2. **更好的状态管理**
   - AgenticScope自动记录所有Agent调用
   - 状态在Agent间高效共享
   - 支持上下文总结

3. **更灵活的组合**
   - 支持顺序、并行、条件、循环工作流
   - 可以轻松添加新Agent
   - 支持Human-in-the-loop

4. **更强的可观测性**
   - 自动记录调用链
   - 每个Agent的输入输出都可追踪
   - 便于调试和优化

---

## 注意事项

### ⚠️ 关键点

1. **@Agent注解是必须的**
   ```java
   @Agent("查询用户订单状态和历史") // 必须有描述
   String queryOrders(...);
   ```

2. **outputName必须配置**
   ```java
   .outputName("orderResult") // 用于AgenticScope共享
   ```

3. **参数使用@V绑定变量**
   ```java
   String query(@V("userId") String userId, @V("request") String request);
   ```

4. **子Agent也是@Agent**
   - 即使是子Agent也需要@Agent注解
   - 描述会被Supervisor用来理解能力

---

## 示例调用流程

### 用户："查询我的订单状态"

**SupervisorAgent规划**：
```
1. 分析用户意图 -> 订单查询
2. 选择子Agent -> OrderAgent
3. 生成执行计划 -> queryOrders(userId="xxx", request="查询订单状态")
4. 调用OrderAgent
5. 获取结果 -> orderResult
6. 返回给用户
```

---

## 文件结构

```
src/main/java/.../agent/
├── agentic/                          # 新增：监督代理相关
│   ├── agents/                       # 子Agent接口
│   │   ├── OrderAgent.java
│   │   ├── RecommendationAgent.java
│   │   ├── NutritionAgent.java
│   │   ├── MerchantAgent.java
│   │   └── UserAgent.java
│   ├── config/                       # 监督代理配置
│   │   └── AgenticSupervisorConfig.java
│   └── supervisor/                   # 监督代理
│       └── IntelligentSupervisor.java
├── tools/                            # 现有工具类（保持不变）
│   ├── order/
│   ├── recommendation/
│   └── ...
└── config/                           # 现有配置（保留）
    └── LangChain4jStreamingConfig.java
```

---

**状态**: ✅ 设计完成，准备实施
