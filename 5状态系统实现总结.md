# 订单5状态系统实现总结

## 📋 状态系统说明

### 5状态定义
```
0 - 待支付
1 - 待接单
2 - 制作中
3 - 已完成
4 - 已取消
```

### 状态流转图
```
待支付(0) → 待接单(1) → 制作中(2) → 已完成(3)
                      ↓
                   已取消(4)
```

## ✅ 后端实现

### 1. Order实体类
**文件**: `JasEatsChoiceJava/src/main/java/com/xx/jaseatschoicejava/entity/Order.java`

```java
@TableField("status")
@ApiModelProperty(value = "订单状态：0-待支付、1-待接单、2-制作中、3-已完成、4-已取消")
private Integer status;
```

### 2. OrderController
**文件**: `JasEatsChoiceJava/src/main/java/com/xx/jaseatschoicejava/controller/OrderController.java`

#### 接单逻辑
- 状态1（待接单）→ 状态2（制作中）
```java
acceptOrder(orderId) → PUT /v1/orders/{orderId}/status?status=2
```

#### 完成订单逻辑
- 状态2（制作中）→ 状态3（已完成）
```java
completeOrder(orderId) → PUT /v1/orders/{orderId}/status?status=3
```

#### 取消订单逻辑
- 状态0或1 → 状态4（已取消）
```java
cancelOrder(orderId, reason) → PUT /v1/orders/{orderId}/cancel?reason={reason}
order.setStatus(4);  // 已取消
```

#### 通知逻辑
```java
switch (status) {
    case 2: // 制作中
        NotificationUtil.createOrderNotification(..., "商家已接单，正在制作中");
        break;
    case 3: // 已完成
        NotificationUtil.createOrderNotification(..., "订单已完成");
        break;
}
```

#### 统计逻辑
```java
// 进行中订单：1-待接单、2-制作中
if (status == 1 || status == 2) {
    inProgress++;
}

// 待确认订单：0-待支付、1-待接单
if (status == 0 || status == 1) {
    pending++;
}

// 5状态系统无待评价概念
statistics.put("pendingComment", 0);
```

## ✅ 前端实现（桌面端）

### orderStatus.js
**文件**: `JasEatsChoiceFront/src/renderer/src/utils/orderStatus.js`

#### 前端状态映射
```javascript
export const ORDER_STATUS_MAP = {
  all: '全部订单',
  pending: '待支付',       // 后端: 0
  pendingAccept: '待接单',  // 后端: 1
  preparing: '制作中',     // 后端: 2
  completed: '已完成',     // 后端: 3
  cancelled: '已取消'      // 后端: 4
}
```

#### 后端状态码到前端状态的映射
```javascript
const BACKEND_STATUS_MAP = {
  0: 'pending',         // 待支付
  1: 'pendingAccept',   // 待接单
  2: 'preparing',       // 制作中
  3: 'completed',       // 已完成
  4: 'cancelled',       // 已取消
  // 兼容旧状态码
  5: 'completed',       // 旧状态：已送达 → 已完成
  6: 'cancelled',       // 旧状态：已取消 → 已取消
  7: 'completed',       // 旧状态：待评价 → 已完成
  8: 'completed'        // 旧状态：已评价 → 已完成
}
```

## ✅ 前端实现（小程序端）

### 1. merchant API
**文件**: `JasEatsChoiceUniApp/src/api/modules/merchant.js`

```javascript
/**
 * 接单 - 将状态更新为2(制作中)
 */
acceptOrder: (orderId) => put(`/v1/orders/${orderId}/status`, null, { params: { status: 2 } })

/**
 * 完成订单 - 将状态更新为3(已完成)
 */
completeOrder: (orderId) => put(`/v1/orders/${orderId}/status`, null, { params: { status: 3 } })

/**
 * 拒单 - 将状态更新为4(已取消)
 */
rejectOrder: (orderId, reason) => put(`/v1/orders/${orderId}/cancel`, null, { params: { reason } })
```

### 2. 订单列表页面（index.vue）
**文件**: `JasEatsChoiceUniApp/src/pages-merchant/order/index.vue`

#### 状态映射
```javascript
const orderStatusMap = {
  0: { text: '待支付', value: 'unpaid' },
  1: { text: '待接单', value: 'pending' },
  2: { text: '制作中', value: 'preparing' },
  3: { text: '已完成', value: 'completed' },
  4: { text: '已取消', value: 'cancelled' }
}
```

#### 状态Tab
```javascript
const statusTabs = ref([
  { label: '全部', value: 'all', count: 0 },
  { label: '待接单', value: 1, count: 0 },
  { label: '制作中', value: 2, count: 0 },
  { label: '已完成', value: 3, count: 0 },
  { label: '已取消', value: 4, count: 0 }
])
```

#### 按钮显示逻辑
```vue
<!-- 接单按钮：status === 1 -->
<button v-if="order.status === 1" @tap.stop="acceptOrder(order)">
  接单
</button>

<!-- 完成按钮：status === 2 -->
<button v-if="order.status === 2" @tap.stop="completeOrder(order)">
  完成
</button>
```

#### 状态计数
```javascript
const updateStatusCounts = (orders) => {
  const counts = { 1: 0, 2: 0, 3: 0, 4: 0 }

  orders.forEach(order => {
    if (counts[order.status] !== undefined) {
      counts[order.status]++
    }
  })

  statusTabs.value[1].count = counts[1] // 待接单
  statusTabs.value[2].count = counts[2] // 制作中
  statusTabs.value[3].count = counts[3] // 已完成
  statusTabs.value[4].count = counts[4] // 已取消
}
```

## 🗄️ 数据库迁移

### 迁移脚本
**文件**: `JasEatsChoiceJava/src/main/resources/db/migration/V2026031901__update_order_status_comment.sql`

```sql
-- 更新订单表状态字段注释为5状态系统
ALTER TABLE `t_order`
MODIFY COLUMN `status` tinyint NOT NULL DEFAULT '0'
COMMENT '订单状态：0-待支付、1-待接单、2-制作中、3-已完成、4-已取消';
```

## 🔄 兼容性处理

### 旧状态码到新状态码的映射
桌面端提供了兼容层，自动将旧状态码映射到新状态：

```javascript
// 旧状态码（0-8）→ 新状态码（0-4）
2, 3, 4 → 2 (制作中)      // 备菜中、烹饪中、待上菜 → 制作中
5, 7, 8 → 3 (已完成)      // 已送达、待评价、已评价 → 已完成
6 → 4 (已取消)            // 已取消 → 已取消
```

## 📊 对比：9状态 vs 5状态

### 9状态系统（已废弃）
```
0-待支付
1-待接单
2-备菜中
3-烹饪中
4-待上菜
5-已送达
6-已取消
7-待评价
8-已评价
```

### 5状态系统（当前）
```
0-待支付
1-待接单
2-制作中     ← 合并了：备菜中、烹饪中、待上菜
3-已完成     ← 合并了：已送达、待评价、已评价
4-已取消
```

## 🎯 优势

1. **简化用户认知**：用户只需理解5个状态，而不是9个
2. **减少开发复杂度**：不需要维护细分的状态转换逻辑
3. **提升流转效率**：减少中间状态的等待和判断
4. **统一商家体验**：商家不需要区分备菜、烹饪、待上菜等细节
5. **兼容旧数据**：前端提供兼容层，自动处理旧状态码

## ⚠️ 注意事项

1. **数据库字段名仍为status**：不需要修改数据库表结构，只修改注释
2. **字段类型保持tinyint**：可以存储0-255的值，足够使用
3. **兼容旧数据**：桌面端的orderStatus.js提供了映射逻辑
4. **API路径不变**：前端调用API的路径保持不变
5. **状态码简化**：只修改了业务逻辑，不修改底层存储

## 🚀 下一步

1. 重启后端服务
2. 执行数据库迁移脚本
3. 测试订单流程：待接单 → 制作中 → 已完成
4. 验证状态统计是否正确
5. 检查通知是否正常发送

## 📝 相关文件清单

### 后端
- ✅ `JasEatsChoiceJava/src/main/java/com/xx/jaseatschoicejava/entity/Order.java`
- ✅ `JasEatsChoiceJava/src/main/java/com/xx/jaseatschoicejava/controller/OrderController.java`
- ✅ `JasEatsChoiceJava/src/main/resources/db/migration/V2026031901__update_order_status_comment.sql`

### 桌面端
- ✅ `JasEatsChoiceFront/src/renderer/src/utils/orderStatus.js`
- ✅ `JasEatsChoiceFront/src/renderer/src/views/merchant/TodayOrders.vue`
- ✅ `JasEatsChoiceFront/src/renderer/src/views/merchant/Orders.vue`
- ✅ `JasEatsChoiceFront/src/renderer/src/views/merchant/OrderDetail.vue`
- ✅ `JasEatsChoiceFront/src/renderer/src/components/merchant/OrderCenter.vue`

### 小程序端
- ✅ `JasEatsChoiceUniApp/src/api/modules/merchant.js`
- ✅ `JasEatsChoiceUniApp/src/pages-merchant/order/index.vue`
- ⚠️ `JasEatsChoiceUniApp/src/pages-merchant/order/today.vue`（待更新）
- ⚠️ `JasEatsChoiceUniApp/src/pages-merchant/order/detail.vue`（待更新）

---

**最后更新时间**: 2026-03-19
**状态**: ✅ 后端和桌面端已完成，小程序端部分完成
