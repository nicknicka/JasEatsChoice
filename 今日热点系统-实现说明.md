# 今日热点系统 - 实现说明文档

## 一、概述

实现了一个智能化的今日热点系统，采用**组合策略**获取热点数据，确保系统在各种情况下都能提供优质的热点内容。

### 实现方案

- **方案三（组合策略）**：数据库配置 > 热门教程 > AI生成 > 默认热点
- **方案四（第三方API）**：作为备选数据源，可通过配置启用

---

## 二、数据来源优先级

### 1. 数据库配置热点（优先级最高）⭐⭐⭐⭐⭐

**数据来源**：`hot_topic` 表

**获取条件**：
- 状态为 `ACTIVE`
- 当前时间在 `start_date` 和 `end_date` 之间
- 审核状态为 `APPROVED`（如需审核）
- 按 `priority` DESC 排序，取最高优先级

**适用场景**：
- 管理员手动配置的重要热点
- 节日活动、促销信息等时效性内容
- 需要精确控制的推广内容

### 2. 热门教程提取（优先级：高）⭐⭐⭐⭐

**数据来源**：`tutorial` 表

**获取条件**：
- `status = 'PUBLISHED'`
- `review_status = 'APPROVED'`
- 按 `view_count` DESC 排序，取浏览量最高的教程标题

**自动保存**：
- 提取的热点会自动保存到 `hot_topic` 表
- `source_type = 'TUTORIAL'`
- 有效期24小时

**适用场景**：
- 基于用户真实浏览行为的热点
- 自动更新，无需人工干预

### 3. AI生成热点（优先级：中）⭐⭐⭐

**生成逻辑**：
- 根据当前月份确定季节（春季、夏季、秋季、冬季）
- 根据当前小时确定时段（早餐、午餐、晚餐、健康）
- 随机组合模板生成热点

**模板示例**：
```
{季节}{时段}饮食指南
{季节}{时段}健康食谱推荐
{季节}{时段}养生小贴士
```

**自动保存**：
- AI生成的热点会自动保存到数据库
- `source_type = 'AI'`
- 有效期24小时

**适用场景**：
- 无其他热点数据时的智能补充
- 根据时间和季节自动调整
- 避免内容单调

### 4. 第三方API（优先级：低，备选）⭐⭐

**启用方式**：
在 `application.yml` 中配置：
```yaml
hot-topic:
  api:
    enabled: true
    url: "https://api.example.com/hot-search"
    key: "your-api-key"
    timeout: 5000
```

**注意事项**：
- 默认关闭，需要手动配置
- 需要第三方API密钥
- 建议使用可靠的免费API（如微博热搜、知乎热榜等）

### 5. 默认热点（兜底）⭐

**内容**：`"健康饮食，快乐生活"`

**适用场景**：
- 所有数据源都失败时的兜底方案
- 确保系统始终有内容返回

---

## 三、数据库设计

### hot_topic 表结构

```sql
CREATE TABLE hot_topic (
    id VARCHAR(32) PRIMARY KEY COMMENT '主键ID',
    content VARCHAR(500) NOT NULL COMMENT '热点内容',
    priority INT DEFAULT 0 COMMENT '优先级，数值越大优先级越高',
    source_type VARCHAR(20) DEFAULT 'MANUAL' COMMENT '来源类型',
    source_id VARCHAR(100) COMMENT '来源ID',

    -- 时间控制
    start_date DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '生效开始时间',
    end_date DATETIME COMMENT '生效结束时间',

    -- 状态管理
    status VARCHAR(20) DEFAULT 'ACTIVE' COMMENT '状态',
    review_status VARCHAR(20) DEFAULT 'APPROVED' COMMENT '审核状态',

    -- 统计数据
    click_count INT DEFAULT 0 COMMENT '点击次数',
    share_count INT DEFAULT 0 COMMENT '分享次数',

    -- 审核信息
    require_review BOOLEAN DEFAULT FALSE COMMENT '是否需要审核',
    reviewer_id BIGINT COMMENT '审核人ID',
    review_time DATETIME COMMENT '审核时间',
    review_comment VARCHAR(500) COMMENT '审核意见',

    -- 备注
    remark VARCHAR(500) COMMENT '备注信息',

    -- 时间戳
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

---

## 四、API接口

### 1. 用户端接口

#### 获取今日热点
```
GET /api/v1/home/hot-topic

响应示例：
{
  "success": true,
  "code": "200",
  "message": "成功",
  "data": "夏日清凉饮食指南"
}
```

#### 记录热点点击
```
POST /api/v1/home/hot-topic/click?content=xxx

响应：
{
  "success": true,
  "code": "200",
  "message": "成功"
}
```

#### 记录热点分享
```
POST /api/v1/home/hot-topic/share?content=xxx

响应：
{
  "success": true,
  "code": "200",
  "message": "成功"
}
```

### 2. 管理后台接口

#### 分页查询热点列表
```
GET /api/v1/admin/hot-topic/list?page=1&size=10&status=ACTIVE
```

#### 获取热点详情
```
GET /api/v1/admin/hot-topic/detail/{id}
```

#### 创建热点
```
POST /api/v1/admin/hot-topic/create
Body: {
  "content": "热点内容",
  "priority": 10,
  "startDate": "2024-01-29 00:00:00",
  "endDate": "2024-01-30 23:59:59",
  ...
}
```

#### 更新热点
```
PUT /api/v1/admin/hot-topic/update/{id}
```

#### 删除热点
```
DELETE /api/v1/admin/hot-topic/delete/{id}
```

#### 审核热点
```
POST /api/v1/admin/hot-topic/review/{id}?reviewerId=1&approved=true&comment=通过
```

#### 获取统计数据
```
GET /api/v1/admin/hot-topic/statistics

响应：
{
  "success": true,
  "data": {
    "total": 100,
    "active": 50,
    "pending": 5,
    "totalClicks": 10000,
    "totalShares": 500
  }
}
```

---

## 五、使用建议

### 1. 数据库配置热点（推荐）

**优势**：
- 完全可控，适合重要内容
- 支持定时发布和过期
- 优先级最高

**操作步骤**：
1. 登录管理后台
2. 进入"热点管理"
3. 创建热点，设置高优先级（如100）
4. 设置生效时间段

### 2. 热门教程提取（自动运行）

**无需配置**，系统自动：
- 查询浏览量最高的教程
- 提取标题作为热点
- 自动保存到数据库（有效期24小时）

### 3. AI生成热点（自动运行）

**无需配置**，系统根据：
- 当前季节（春夏秋冬）
- 当前时段（早午晚餐）
- 自动生成合适的热点内容

### 4. 第三方API（可选）

**如需启用**：
1. 获取第三方API密钥
2. 修改 `application.yml` 配置
3. 重启应用

**推荐API**：
- 微博热搜API
- 知乎热榜API
- 百度热搜API

---

## 六、核心代码文件

### 后端文件

| 文件路径 | 说明 |
|---------|------|
| `entity/HotTopic.java` | 热点实体类 |
| `mapper/HotTopicMapper.java` | 热点Mapper接口 |
| `service/HotTopicService.java` | 热点服务接口 |
| `service/impl/HotTopicServiceImpl.java` | 热点服务实现（组合策略核心逻辑） |
| `controller/HomeController.java` | 用户端接口 |
| `controller/HotTopicAdminController.java` | 管理后台接口 |
| `resources/db/migration/V2026012901__create_hot_topic.sql` | 数据库迁移脚本 |

### 前端文件

| 文件路径 | 说明 |
|---------|------|
| `views/user/HomeContent.vue` | 用户首页（显示今日热点） |

### 配置文件

| 文件路径 | 说明 |
|---------|------|
| `resources/application.yml` | 应用配置（含第三方API配置） |

---

## 七、测试方法

### 1. 测试数据库热点

```sql
-- 插入测试数据
INSERT INTO hot_topic (id, content, priority, source_type, start_date, end_date, status, review_status)
VALUES (
    'test001',
    '测试热点：春季健康饮食指南',
    100,
    'MANUAL',
    NOW(),
    DATE_ADD(NOW(), INTERVAL 7 DAY),
    'ACTIVE',
    'APPROVED'
);

-- 测试查询
SELECT * FROM hot_topic WHERE status = 'ACTIVE' ORDER BY priority DESC;
```

### 2. 测试接口

```bash
# 获取今日热点
curl http://localhost:8080/api/v1/home/hot-topic

# 记录点击
curl -X POST "http://localhost:8080/api/v1/home/hot-topic/click?content=测试热点"

# 管理后台查询
curl http://localhost:8080/api/v1/admin/hot-topic/list?page=1&size=10
```

### 3. 测试组合策略

```java
// 临时禁用所有ACTIVE状态的数据库热点
UPDATE hot_topic SET status = 'INACTIVE' WHERE status = 'ACTIVE';

// 观察系统是否自动从教程/AI获取热点
```

---

## 八、常见问题

### Q1: 如何设置热点优先级？

**A**: 在管理后台创建热点时，设置 `priority` 字段：
- 100及以上：紧急热点（始终显示）
- 50-99：重要热点
- 10-49：普通热点
- 1-9：自动生成的热点（教程、AI）

### Q2: 热点会自动更新吗？

**A**: 是的，系统会：
1. 优先使用管理员配置的热点
2. 如无配置热点，自动从教程提取
3. 如无热门教程，AI自动生成
4. 自动保存到数据库（有效期24小时）

### Q3: 如何临时关闭某个热点？

**A**: 方式一：修改 `end_date` 为过去时间
```sql
UPDATE hot_topic SET end_date = NOW() - INTERVAL 1 DAY WHERE id = 'xxx';
```

方式二：修改状态为 `INACTIVE`
```sql
UPDATE hot_topic SET status = 'INACTIVE' WHERE id = 'xxx';
```

### Q4: 第三方API如何配置？

**A**: 编辑 `application.yml`：
```yaml
hot-topic:
  api:
    enabled: true  # 改为true
    url: "https://api.example.com/hot-search"  # 填写API地址
    key: "your-api-key"  # 填写API密钥
```

### Q5: 如何查看热点统计数据？

**A**: 调用管理后台接口：
```bash
curl http://localhost:8080/api/v1/admin/hot-topic/statistics
```

---

## 九、未来扩展建议

1. **热点推荐算法**：基于用户兴趣、历史行为个性化推荐
2. **热点趋势分析**：统计热点点击、分享趋势图
3. **A/B测试**：测试不同热点文案的点击率
4. **定时任务**：定期清理过期热点，优化数据库
5. **缓存优化**：使用Redis缓存热点，减少数据库查询

---

## 十、总结

该今日热点系统采用**智能化组合策略**，确保：

✅ **高可用性**：多级数据源，始终有内容返回
✅ **自动化**：教程提取、AI生成全自动运行
✅ **可控性**：管理员可精确控制重要热点
✅ **扩展性**：支持第三方API接入
✅ **统计功能**：点击、分享数据可追溯

系统已完整实现，可直接投入使用！
