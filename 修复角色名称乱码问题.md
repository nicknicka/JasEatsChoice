# 管理员角色名称乱码问题修复指南

## 问题描述
管理员后台显示的"角色名称"显示为乱码（如："è¶…çº§ç®¡ç†å'"），这是数据库字符集编码问题。

## 问题原因
1. 数据库表的字符集可能不是UTF-8
2. 数据库连接的字符编码设置可能没有正确生效
3. 历史数据可能使用了错误的编码存储

## 解决方案

### 方案一：修复数据库（推荐）

#### 1. 备份数据库
```bash
mysqldump -u root -p jia_shi_yi_xuan > backup_$(date +%Y%m%d).sql
```

#### 2. 执行修复脚本
```bash
mysql -u root -p jia_shi_yi_xi_xuan < fix_admin_charset.sql
```

#### 3. 检查角色数据
登录MySQL：
```bash
mysql -u root -p jia_shi_yi_xi_xuan
```

执行查询：
```sql
-- 查看角色表数据
SELECT * FROM t_role;

-- 如果发现乱码，更新角色名称
UPDATE t_role SET role_name = '超级管理员' WHERE role_code = 'SUPER_ADMIN';
UPDATE t_role SET role_name = '管理员' WHERE role_code = 'ADMIN';
```

#### 4. 验证修复
刷新管理员页面，角色名称应该正常显示

---

### 方案二：前端映射（临时方案）

前端已经添加了角色名称映射配置：
- 文件位置：`src/renderer/src/config/index.js`
- 映射关系：`ROLE_CODE -> 正确的中文名称`

**映射配置：**
```javascript
export const ROLE_NAME_MAP = {
  'SUPER_ADMIN': '超级管理员',
  'ADMIN': '管理员',
  'USER_MANAGER': '用户管理员',
  'MERCHANT_MANAGER': '商家管理员',
  'CONTENT_MANAGER': '内容管理员',
  'FINANCE_MANAGER': '财务管理员'
}
```

**自动修复逻辑：**
- 检测到roleName为乱码时，自动使用映射表中的中文名称
- 检测条件：包含非打印字符或长度超过50

---

### 方案三：后端强制编码（彻底方案）

修改Java代码，确保返回正确的UTF-8编码：

#### 1. 在application.yml中添加消息转换器配置
```yaml
spring:
  jackson:
    default-property-inclusion: non_null
    serialization:
      write-dates-as-timestamps: false
  http:
    encoding:
      charset: UTF-8
      enabled: true
      force: true
      force-request: true
      force-response: true
```

#### 2. 创建配置类强制UTF-8编码
```java
@Configuration
public class CharsetConfig {

    @Bean
    public HttpMessageConverter<String> responseBodyConverter() {
        StringHttpMessageConverter converter = new StringHttpMessageConverter(
            StandardCharsets.UTF_8
        );
        return converter;
    }

    @Bean
    public Charset charset() {
        return StandardCharsets.UTF_8;
    }
}
```

---

## 验证步骤

### 1. 检查数据库字符集
```sql
SHOW CREATE TABLE t_role;
SHOW CREATE TABLE t_admin;
```

### 2. 检查连接字符串
确保JDBC URL包含：
```
useUnicode=true&characterEncoding=UTF-8
```

### 3. 测试API响应
使用Postman或curl测试API：
```bash
curl -H "Authorization: Bearer YOUR_TOKEN" \
     http://localhost:8080/api/admin/current
```

### 4. 检查响应头
确保返回的Content-Type包含charset：
```
Content-Type: application/json;charset=UTF-8
```

---

## 快速修复（立即可用）

### 步骤1：直接更新数据库
```sql
USE jia_shi_yi_xi_xuan;

-- 更新角色名称
UPDATE t_role SET role_name = '超级管理员' WHERE role_code = 'SUPER_ADMIN';

-- 验证
SELECT role_id, role_code, role_name FROM t_role;
```

### 步骤2：刷新前端页面
清除浏览器缓存并刷新

### 步骤3：验证结果
- ✅ 角色名称应显示为"超级管理员"
- ✅ 不再出现乱码

---

## 长期预防措施

### 1. 确保所有表使用UTF-8
```sql
-- 为新表指定字符集
CREATE TABLE table_name (
    ...
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

### 2. 数据库连接配置
```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/db_name?useUnicode=true&characterEncoding=UTF-8
```

### 3. Java代码配置
确保所有Controller返回时明确指定编码：
```java
@GetMapping(value = "/api/xxx", produces = "application/json;charset=UTF-8")
```

---

## 相关文件

- 前端映射配置：`src/renderer/src/config/index.js`
- 前端组件：`src/renderer/src/components/AdminLayout.vue`
- 数据库脚本：`fix_admin_charset.sql`
- 后端Controller：`AdminController.java`

---

## 技术支持

如果问题仍然存在，请检查：
1. MySQL服务器字符集配置：`SHOW VARIABLES LIKE 'character%';`
2. 数据库字符集配置：`SHOW VARIABLES LIKE 'collation%';`
3. 表的字符集：`SHOW TABLE STATUS;`
