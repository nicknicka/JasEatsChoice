# 组件重构建议

## 📊 当前状态分析

### ✅ 已做得很好的地方

**全局通用组件**（`src/components/common/`）- 8个
- Card.vue ✅
- Loading.vue ✅
- Empty.vue ✅
- ListItem.vue ✅
- Button.vue ✅
- Badge.vue ✅
- Tag.vue ✅
- WeatherLocation.vue ✅

这些组件确实被多个页面使用，放在全局完全正确。

### ⚠️ 需要调整的问题

#### **问题1: 缺少业务组件目录**

项目中缺少 **`src/components/business/`** 目录来存放跨页面的业务组件。

**影响**：导致多个页面重复实现相同的UI模式
- 菜品卡片在首页、收藏、商家详情、搜索等多个页面重复
- 商家卡片在多个页面重复
- 优惠券卡片在优惠券页和订单确认页重复

#### **问题2: 部分页面组件过大**

以下页面代码量过大，需要拆分：

| 页面 | 行数 | 问题 |
|------|------|------|
| order/progress/index.vue | 1082行 | 时间轴、地图、状态卡片混在一起 |
| wallet/index.vue | 952行 | 充值弹窗、交易列表混在一起 |
| recipe/detail/index.vue | 824行 | 食材、步骤、营养信息混在一起 |
| user-center/edit/index.vue | 617行 | 多个选择器逻辑混在一起 |

## 🎯 重构方案

### **阶段1: 创建业务组件目录** ⭐ 优先级最高

```bash
src/components/
├── common/              # ✅ 已有（通用基础组件）
│   ├── Card.vue
│   ├── Loading.vue
│   ├── Empty.vue
│   ├── ListItem.vue
│   ├── Button.vue
│   ├── Badge.vue
│   └── Tag.vue
│
└── business/            # ✨ 新增（业务组件）
    ├── DishCard.vue         # 菜品卡片 ✨已创建
    ├── MerchantCard.vue     # 商家卡片 ✨已创建
    ├── OrderCard.vue        # 订单卡片
    ├── CouponCard.vue       # 优惠券卡片
    ├── ReviewItem.vue       # 评价项
    └── NutritionBar.vue     # 营养成分条
```

**创建的业务组件**：

1. **DishCard.vue** - 菜品卡片
   - 使用场景：首页、收藏页、商家详情页、搜索页、订单确认页
   - 功能：显示菜品图片、名称、价格、标签、收藏按钮
   - Props: dish, showMerchant, showAddBtn
   - Events: click, favorite, add

2. **MerchantCard.vue** - 商家卡片
   - 使用场景：首页、收藏页、搜索页
   - 功能：显示商家图片、名称、评分、标签、距离
   - Props: merchant
   - Events: click, favorite

3. **OrderCard.vue** - 订单卡片（待创建）
   - 使用场景：订单列表、订单详情
   - 功能：显示订单状态、菜品信息、价格、操作按钮

4. **CouponCard.vue** - 优惠券卡片（待创建）
   - 使用场景：我的优惠券、订单确认页
   - 功能：显示优惠券金额、条件、有效期、使用按钮

5. **ReviewItem.vue** - 评价项（待创建）
   - 使用场景：评价列表、菜品详情
   - 功能：显示用户头像、评分、内容、图片

6. **NutritionBar.vue** - 营养成分条（待创建）
   - 使用场景：食谱详情、卡路里管理
   - 功能：显示营养成分进度条

**收益**：
- ✅ 减少50%以上的重复代码
- ✅ 统一UI风格
- ✅ 便于维护和修改
- ✅ 提高开发效率

### **阶段2: 拆分大页面组件** ⭐ 优先级高

#### **2.1 拆分 order/progress/index.vue (1082行 → 3个组件)**

```bash
src/pages/order/progress/
├── index.vue                    # 主页面（~300行）
└── components/                  # ✨ 新增
    ├── OrderStatusCard.vue      # 状态卡片（~150行）
    ├── TimelineSection.vue      # 时间轴（~300行）
    ├── DeliveryInfo.vue         # 配送信息（~200行）
    └── OrderDetail.vue          # 订单详情（~150行）
```

**OrderStatusCard.vue**
- 动态渐变色背景
- 状态图标和文字
- 预计送达时间

**TimelineSection.vue**
- 时间轴步骤列表
- 节点动画效果
- 步骤详情展示

**DeliveryInfo.vue**
- 骑手信息卡片
- 配送地址
- 地图展示

#### **2.2 拆分 wallet/index.vue (952行 → 3个组件)**

```bash
src/pages/wallet/
├── index.vue                    # 主页面（~250行）
└── components/                  # ✨ 新增
    ├── WalletCard.vue           # 钱包卡片（~100行）
    ├── TransactionList.vue      # 交易列表（~400行）
    └── RechargePopup.vue        # 充值弹窗（~300行）
```

**WalletCard.vue**
- 余额显示（整数+小数）
- 充值/提现按钮
- 渐变背景设计

**TransactionList.vue**
- 交易记录列表
- 日期分组
- 收入/支出筛选

**RechargePopup.vue**
- 金额输入
- 快捷金额选择
- 支付方式选择

#### **2.3 拆分 recipe/detail/index.vue (824行 → 4个组件)**

```bash
src/pages/recipe/detail/
├── index.vue                    # 主页面（~300行）
└── components/                  # ✨ 新增
    ├── RecipeHeader.vue         # 头部大图（~100行）
    ├── IngredientList.vue       # 食材列表（~200行）
    ├── CookSteps.vue            # 烹饪步骤（~150行）
    └── NutritionInfo.vue        # 营养信息（~100行）
```

**RecipeHeader.vue**
- 大图展示
- 收藏/分享按钮
- 渐变遮罩

**IngredientList.vue**
- 食材分组
- 勾选功能
- 数量显示

**CookSteps.vue**
- 步骤编号
- 步骤描述
- 图片展示

**NutritionInfo.vue**
- 营养成分卡片
- 数值展示
- 图标化设计

#### **2.4 拆分 user-center/edit/index.vue (617行 → 4个组件)**

```bash
src/pages/user-center/edit/
├── index.vue                    # 主页面（~200行）
└── components/                  # ✨ 新增
    ├── AvatarUploader.vue       # 头像上传（~100行）
    ├── GenderPicker.vue         # 性别选择（~80行）
    ├── TasteSelector.vue        # 口味选择（~120行）
    └── AllergySelector.vue      # 过敏原选择（~120行）
```

**AvatarUploader.vue**
- 头像预览
- 拍照/相册选择
- 上传处理

**GenderPicker.vue**
- 单选组件
- 3个选项

**TasteSelector.vue**
- 多选组件
- 口味标签

**AllergySelector.vue**
- 多选组件
- 过敏原标签

### **阶段3: 优化已创建页面**

在以下页面中使用新创建的业务组件：

1. **collection/index.vue** - 使用 DishCard 和 MerchantCard
2. **history/index.vue** - 使用 DishCard
3. **index/index.vue** - 使用 DishCard 和 MerchantCard
4. **search/index.vue** - 使用 DishCard 和 MerchantCard
5. **merchant/detail/index.vue** - 使用 DishCard
6. **coupon/index.vue** - 使用 CouponCard（待创建）

## 📝 重构步骤

### Step 1: 创建业务组件（已完成 ✅）
```bash
✅ src/components/business/DishCard.vue
✅ src/components/business/MerchantCard.vue
⏳ src/components/business/OrderCard.vue
⏳ src/components/business/CouponCard.vue
⏳ src/components/business/ReviewItem.vue
⏳ src/components/business/NutritionBar.vue
```

### Step 2: 在页面中使用业务组件

**示例：在收藏页面使用 DishCard**

```vue
<!-- src/pages/collection/index.vue -->
<template>
  <view class="dish-grid">
    <DishCard
      v-for="dish in dishList"
      :key="dish.id"
      :dish="dish"
      :showMerchant="true"
      @click="viewDishDetail"
      @favorite="toggleFavorite"
    />
  </view>
</template>

<script setup>
import DishCard from '@/components/business/DishCard.vue'
// ...
</script>
```

### Step 3: 拆分大页面组件

按顺序拆分：
1. order/progress/index.vue
2. wallet/index.vue
3. recipe/detail/index.vue
4. user-center/edit/index.vue

### Step 4: 测试和验证

- 功能测试：确保所有功能正常
- 样式测试：确保UI一致
- 性能测试：确保没有性能退化

## 🎯 预期收益

### 代码质量提升
- ✅ 减少 50% 以上的重复代码
- ✅ 单个文件代码量控制在 400 行以内
- ✅ 组件复用率提升至 60% 以上

### 开发效率提升
- ✅ 新功能开发速度提升 30%
- ✅ Bug 修复更快速
- ✅ UI 调整更统一

### 可维护性提升
- ✅ 代码结构更清晰
- ✅ 组件职责更单一
- ✅ 便于团队协作

## ⚠️ 注意事项

### 1. 重构原则
- **先创建，后替换**：先创建新组件，再逐步替换旧代码
- **小步迭代**：一次只重构一个页面
- **保持功能**：重构过程中功能不能受影响
- **及时测试**：每次重构后立即测试

### 2. 组件设计原则
- **单一职责**：一个组件只做一件事
- **Props down, Events up**：数据向下传递，事件向上冒泡
- **可复用性**：通过 props 控制组件行为
- **命名规范**：使用语义化的命名

### 3. 样式处理
- 使用全局变量和 mixins
- 保持设计系统一致性
- 避免样式冲突

## 📅 实施计划

| 阶段 | 任务 | 预计时间 | 优先级 |
|------|------|----------|--------|
| 阶段1 | 创建业务组件 | 2小时 | ⭐⭐⭐ |
| 阶段2 | 拆分大页面组件 | 4小时 | ⭐⭐ |
| 阶段3 | 优化已有页面 | 2小时 | ⭐ |
| 阶段4 | 测试和验证 | 1小时 | ⭐⭐⭐ |

**总计**：约 9 小时

## ✅ 下一步行动

建议按以下顺序执行：

1. **立即执行**：创建剩余的业务组件（OrderCard, CouponCard等）
2. **本周内**：拆分最大的 2 个页面（order/progress, wallet）
3. **下周**：拆分剩余 2 个页面（recipe/detail, user-center/edit）
4. **持续优化**：在后续开发中遵循新的组件组织方式

---

通过这次重构，项目的组件结构将更加清晰合理，为后续开发打下良好基础！
