# 监督代理架构修复执行清单

**日期**: 2026-03-25
**预估总工时**: 6-9小时
**优先级**: P0（必须完成）

---

## 🚀 快速启动

### 一句话总结
**将所有Agent从 `AiServices.builder()` 改为 `AgenticServices.agentBuilder()`，并在Supervisor中调用 `.subAgents()` 注册子Agent。**

---

## ✅ 修改清单

### 文件1: `LangChain4jConfig.java`

#### 🔴 必须修改（P0）

**1.1 添加双模型配置**
```java
// 在类开头添加
@Bean("supervisorModel")
public ChatModel supervisorModel() {
    return ZhipuAiChatModel.builder()
        .apiKey(zhipuAIConfig.getApiKey())
        .model("glm-4-plus")  // 强模型
        .temperature(0.3)
        .build();
}

@Bean("agentModel")
public ChatModel agentModel() {
    return ZhipuAiChatModel.builder()
        .apiKey(zhipuAIConfig.getApiKey())
        .model("glm-4-flash")  // 快速模型
        .temperature(0.7)
        .build();
}
```

**1.2 修改所有L2 Agent（7个）**

需要修改的方法：
- [ ] `smartRecommendationAgent()` - L479
- [ ] `healthManagementAgent()` - L504
- [ ] `fullOrderAgent()` - L525
- [ ] `intelligentAssistantAgent()` - L550
- [ ] `lifeServiceAgent()` - L582
- [ ] `dailyPlanningAgent()` - L610
- [ ] `goalAchievementAgent()` - L633

**修改模板**：
```java
// 修改前
return AiServices.builder(SmartRecommendationAgent.class)
    .chatModel(chatLanguageModel)
    .chatMemory(chatMemory)
    .tools(...)
    .build();

// 修改后
return AgenticServices.agentBuilder(SmartRecommendationAgent.class)
    .chatModel(agentModel)  // 使用 @Qualifier("agentModel")
    .chatMemory(chatMemory)
    .tools(...)
    .description("""
        智能推荐专家，负责个性化菜品推荐。

        **何时调用：**
        - 用户想要菜品推荐
        - 用户询问"吃什么"
        """)
    .outputKey("recommendations")
    .build();
```

**1.3 修改所有L1 Agent（7个）**

需要修改的方法：
- [ ] `userPreferenceAgent()` - L351
- [ ] `nutritionGuideAgent()` - L368
- [ ] `dishRecommendationAgent()` - L386
- [ ] `merchantInfoAgent()` - L405
- [ ] `timeAwareAgent()` - L422
- [ ] `locationServiceAgent()` - L440
- [ ] `orderHelperAgent()` - L457

**修改模板**：
```java
return AgenticServices.agentBuilder(UserPreferenceAgent.class)
    .chatModel(agentModel)
    .chatMemory(chatMemory)
    .tools(...)
    .description("查询用户偏好...")
    .outputKey("userPreferences")
    .build();
```

**1.4 修改Supervisor注册子Agent**

在 `supervisorAgent()` 方法中：

```java
// 1. 添加方法参数
public SupervisorAgent supervisorAgent(
        @Qualifier("supervisorModel") ChatModel supervisorModel,  // 改这里
        ChatMemory chatMemory) {

    // 2. 在构建时添加 .subAgents()
    return AgenticServices
        .supervisorBuilder(SupervisorAgent.class)
        .chatModel(supervisorModel)
        .chatMemoryProvider(memoryId -> chatMemory)
        .name("SupervisorAgent")
        .description("智能调度Agent...")
        .subAgents(  // ✅ 添加这一段
            smartRecommendationAgent,
            healthManagementAgent,
            fullOrderAgent,
            intelligentAssistantAgent
        )  // ✅ 结束
        .outputKey("supervisorResult")
        .supervisorContext("...")
        .contextGenerationStrategy(
            SupervisorContextStrategy.CHAT_MEMORY_AND_SUMMARIZATION
        )
        .responseStrategy(
            SupervisorResponseStrategy.SCORED
        )
        .maxAgentsInvocations(10)
        .build();
}
```

---

### 文件2: `AgentMonitoringConfig.java`

#### 🟡 建议修改（P1）

**修改默认启用**:
```java
@ConditionalOnProperty(
    name = "agent.monitoring.enabled",
    havingValue = "true",
    matchIfMissing = true  // 改为 true
)
```

---

### 文件3: `AgentMonitoringController.java`

#### 🟡 建议修改（P1）

**取消注释**:
```java
@RestController  // 取消注释
@RequestMapping("/api/admin/agent-monitoring")
public class AgentMonitoringController {
    ...
}
```

---

### 文件4: `application.yml`

#### 🟢 可选修改（P2）

**添加配置**:
```yaml
agent:
  monitoring:
    enabled: true  # 显式启用监控
```

---

## 🧪 测试验证

### 测试1: 编译测试
```bash
./mvnw clean compile
```
**预期**: BUILD SUCCESS

### 测试2: 启动测试
```bash
./mvnw spring-boot:run
```
**预期**:
- 启动成功
- 日志显示"构建SupervisorAgent（监督代理）..."
- 无ERROR日志

### 测试3: 功能测试
```bash
# 使用curl或Postman测试
curl -X POST http://localhost:8080/api/agent/supervisor/chat \
  -H "Content-Type: application/json" \
  -d '{"message": "推荐健康的午餐"}'
```

**预期**:
- 返回推荐结果
- 日志显示Agent调用链
- 响应时间 < 10秒

---

## 📊 进度跟踪

| 阶段 | 任务 | 状态 | 耗时 |
|------|------|------|------|
| P0-1 | 双模型配置 | ⬜ 待开始 | - |
| P0-2 | L2 Agent修改（7个） | ⬜ 待开始 | - |
| P0-3 | L1 Agent修改（7个） | ⬜ 待开始 | - |
| P0-4 | Supervisor注册子Agent | ⬜ 待开始 | - |
| P1-1 | 启用监控系统 | ⬜ 待开始 | - |
| P1-2 | 配置AgenticScope | ⬜ 待开始 | - |
| 测试 | 功能验证 | ⬜ 待开始 | - |

---

## ⚠️ 注意事项

### 1. 导入语句
确保添加了正确的导入：
```java
import dev.langchain4j.agentic.AgenticServices;
import org.springframework.beans.factory.annotation.Qualifier;
```

### 2. 方法参数
修改Bean方法时，记得添加参数：
```java
// 修改前
public SmartRecommendationAgent smartRecommendationAgent(
        ChatModel chatLanguageModel,  // 旧参数名

// 修改后
public SmartRecommendationAgent smartRecommendationAgent(
        @Qualifier("agentModel") ChatModel agentModel,  // 新参数
```

### 3. Bean注入顺序
SupervisorAgent依赖其他Agent，确保这些Agent先定义。

### 4. 回滚方案
如果启动失败，可以快速回滚：
```bash
git checkout src/main/java/.../LangChain4jConfig.java
```

---

## 🎯 完成标准

- [x] 所有Agent使用 `AgenticServices.agentBuilder()` 构建
- [x] Supervisor成功注册子Agent
- [x] 应用启动成功无错误
- [x] 测试场景通过
- [x] 监控系统工作正常

---

## 📞 支持

如遇问题，参考：
- 详细分析：`监督代理架构实施问题分析报告.md`
- 设计文档：`监督代理架构设计.md`
- 实施计划：`supervisor-agent-implementation-plan.md`

---

**准备开始修改了吗？需要我帮你逐步实施吗？**
