# 个人信息页面数据获取问题诊断报告

## 🔍 问题描述
进入个人信息页面时，无法获取到手机号、邮箱、账号状态、创建时间等信息。

---

## 🐛 问题根源

### 1. 数据流程分析

#### 原始流程（有问题）
```
页面加载 → onMounted()
         → initProfile()
         → getAdminInfo()  // 从 localStorage 读取
         → Object.assign() // 赋值给表单
         → 显示旧数据 ❌
```

**问题点**：
- 用户在我们修改后端代码**之前**就已经登录
- localStorage 中存储的是**旧数据**（缺少 phone、email、status、createTime）
- 页面加载时**不会主动**调用后端接口刷新数据

---

## ✅ 解决方案

### 1. 修改前端初始化逻辑

**文件**: `JasEatsChoiceFront/src/renderer/src/views/admin/Profile.vue`

**修改内容**:
```javascript
// 修改前：只从 localStorage 读取
const initProfile = () => {
  const adminInfo = getAdminInfo()
  if (adminInfo) {
    Object.assign(profileForm, adminInfo)
  }
}

// 修改后：优先从后端获取最新数据
const initProfile = async () => {
  try {
    // 1. 优先从后端获取最新数据
    const response = await api.get('/admin/current')
    if (response.success && response.admin) {
      // 2. 更新 localStorage
      localStorage.setItem('admin_info', JSON.stringify(response.admin))
      // 3. 更新表单
      Object.assign(profileForm, response.admin)
      console.log('[个人信息] 从后端加载最新管理员信息:', response.admin)
    }
  } catch (error) {
    // 4. 出错时降级到 localStorage
    const adminInfo = getAdminInfo()
    if (adminInfo) {
      Object.assign(profileForm, adminInfo)
    }
  }
}
```

### 2. 新的数据流程

```
页面加载 → onMounted()
         → initProfile()
         → api.get('/admin/current')  // 调用后端接口
         → 获取最新完整数据
         → 更新 localStorage
         → 更新表单显示
         → 显示所有字段 ✅
```

---

## 📋 后端接口字段完整性检查

### 接口: `/admin/current`

**文件**: `AdminController.java:206-215`

| 字段 | 是否返回 | 代码行 | 默认值 |
|------|---------|--------|--------|
| adminId | ✅ | 206 | - |
| username | ✅ | 207 | "" |
| realName | ✅ | 208 | "管理员" |
| phone | ✅ | 209 | "" |
| email | ✅ | 210 | "" |
| avatar | ✅ | 211 | "" |
| roleCode | ✅ | 212 | "" |
| roleName | ✅ | 213 | "超级管理员" |
| status | ✅ | 214 | "ACTIVE" |
| createTime | ✅ | 215 | "" |

**结论**: ✅ 后端接口字段完整

---

## 🔄 完整数据流

### 1. 登录时存储数据
```
用户登录 → /admin/login
        → 后端返回完整字段
        → setAdminInfo(response.admin)
        → 存储到 localStorage
```

### 2. 页面初始化获取数据（修复后）
```
进入页面 → onMounted()
        → initProfile()
        → 调用 /admin/current
        → 获取最新数据
        → 更新 localStorage
        → 显示完整信息
```

### 3. 保存修改后更新数据
```
保存修改 → /admin/profile
        → 更新数据库
        → 调用 /admin/current
        → 获取最新数据
        → 更新 localStorage
```

---

## 📝 测试步骤

1. **重启后端服务**（应用后端代码修改）
   ```bash
   # 停止后端服务
   # 重新启动后端应用
   ```

2. **清除旧数据**（可选）
   - 打开浏览器开发者工具 (F12)
   - Application → Local Storage
   - 删除 `admin_info` 和 `admin_token`

3. **重新登录**
   - 使用管理员账号登录
   - 登录时会存储完整数据

4. **访问个人信息页面**
   - 页面会自动调用 `/admin/current` 接口
   - 获取并显示所有字段

---

## ✅ 预期结果

访问个人信息页面时，应该能看到：

- ✅ 基本信息
  - 用户名
  - 真实姓名
  - 手机号
  - 邮箱
  - 头像

- ✅ 角色信息
  - 角色名称
  - 角色代码
  - 账号状态（带悬浮提示）
  - 创建时间

---

## 🔧 技术要点

1. **优先级策略**: 后端接口 > localStorage 缓存
2. **降级策略**: 后端获取失败时，使用 localStorage 数据
3. **数据同步**: 每次进入页面都从后端获取最新数据
4. **错误处理**: 捕获异常，确保页面不会崩溃

---

## 📊 数据存储位置

| 存储位置 | 用途 | 更新时机 |
|---------|------|---------|
| MySQL 数据库 | 持久化存储 | 保存修改时 |
| localStorage | 前端缓存 | 登录/进入页面/保存修改时 |

---

## 🎯 总结

**问题**: 页面初始化时只从旧缓存读取数据
**解决**: 页面加载时主动调用后端接口获取最新数据
**结果**: 所有字段都能正常显示

---

生成时间: 2026-02-10
