# 数据库迁移完成报告

## 迁移时间
- 执行时间：2025年1月24日
- 执行人员：系统管理员
- 数据库：MySQL 9.4.0

## 已创建的数据库对象

### 1. 数据表（5张）

| 表名 | 用途 | 记录数 |
|------|------|--------|
| `user_behavior` | 用户行为记录表 | 0 |
| `user_profile` | 用户画像表 | 0 |
| `dish_features` | 菜品特征表 | 5 |
| `recommendation_log` | 推荐记录表 | 0 |
| `dish_similarity` | 菜品相似度表 | 0 |

### 2. 视图（2个）

| 视图名 | 用途 |
|--------|------|
| `v_user_behavior_stats` | 用户行为统计视图 |
| `v_dish_popularity` | 菜品热度统计视图 |

### 3. 存储过程（1个）

| 存储过程名 | 用途 |
|-----------|------|
| `sp_calculate_dish_popularity` | 计算菜品热度分数 |

### 4. 表结构优化

| 表名 | 新增字段 |
|------|----------|
| `t_dish` | view_count, order_count, favorite_count, avg_rating, tags |

## 初始化数据

- ✅ 5个菜品的特征数据已初始化
- ✅ 所有索引已创建
- ✅ 视图已创建并测试
- ✅ 存储过程已创建并测试

## 数据库对象详情

### user_behavior表结构
```
- id: 主键
- user_id: 用户ID
- behavior_type: 行为类型 (view/click/order/favorite/reject/share)
- item_type: 物品类型 (dish/merchant/recipe)
- item_id: 物品ID
- context: 上下文信息 (JSON)
- duration: 行为持续时长
- created_time: 行为时间

索引：
- idx_user_time (user_id, created_time)
- idx_item_time (item_id, created_time)
- idx_behavior_type (behavior_type)
- idx_user_behavior_type (user_id, behavior_type, created_time)
```

### user_profile表结构
```
- id: 主键
- user_id: 用户ID (唯一)
- preference_tags: 偏好标签 (JSON)
- dietary_restrictions: 饮食禁忌 (JSON)
- flavor_preference: 口味偏好 (JSON)
- price_preference: 价格偏好 (JSON)
- nutrition_goals: 营养目标 (JSON)
- meal_pattern: 用餐模式 (JSON)
- statistics: 统计数据 (JSON)
- last_updated: 最后更新时间

索引：
- idx_user_id (user_id)
```

### dish_features表结构
```
- id: 主键
- dish_id: 菜品ID (唯一)
- category: 菜品分类
- tags: 标签数组 (JSON)
- flavor_profile: 风味特征 (JSON)
- nutrition_info: 营养信息 (JSON)
- ingredients: 主要食材 (JSON)
- cooking_method: 烹饪方式
- suitable_scenarios: 适用场景 (JSON)
- time_period_tags: 时段标签 (JSON)
- season_tags: 季节标签 (JSON)
- price_level: 价格等级 (1-5)
- popularity_score: 热度分数 (0-1)
- created_time: 创建时间
- updated_time: 更新时间

索引：
- idx_category (category)
- idx_popularity (popularity_score DESC)
- idx_dish_id (dish_id)
```

### recommendation_log表结构
```
- id: 主键
- user_id: 用户ID
- recommendation_id: 推荐批次ID
- dish_id: 菜品ID
- rank_position: 推荐排序位置
- score: 推荐得分
- algorithm: 使用的算法
- reason: 推荐理由 (JSON)
- is_clicked: 是否被点击
- is_ordered: 是否被下单
- feedback_time: 反馈时间
- created_time: 推荐时间

索引：
- idx_user_id (user_id)
- idx_recommendation_id (recommendation_id)
- idx_created_time (created_time)
- idx_user_recommendation (user_id, recommendation_id)
```

### dish_similarity表结构
```
- id: 主键
- dish_id_a: 菜品A ID
- dish_id_b: 菜品B ID
- similarity_score: 相似度分数 (0-1)
- similarity_type: 相似度类型 (content/collaborative/hybrid)
- updated_time: 更新时间

约束和索引：
- UNIQUE KEY uk_dish_pair_type (dish_id_a, dish_id_b, similarity_type)
- INDEX idx_similarity_score (similarity_score DESC)
- INDEX idx_dish_a (dish_id_a)
- INDEX idx_dish_b (dish_id_b)
```

## 验证命令

### 检查表是否存在
```sql
SHOW TABLES LIKE '%user_behavior%';
SHOW TABLES LIKE '%user_profile%';
SHOW TABLES LIKE '%dish_features%';
SHOW TABLES LIKE '%recommendation_log%';
SHOW TABLES LIKE '%dish_similarity%';
```

### 查看表结构
```sql
DESCRIBE user_behavior;
DESCRIBE user_profile;
DESCRIBE dish_features;
DESCRIBE recommendation_log;
DESCRIBE dish_similarity;
```

### 查看表记录数
```sql
SELECT 
  'user_behavior' as table_name,
  COUNT(*) as row_count
FROM user_behavior
UNION ALL
SELECT 'user_profile', COUNT(*) FROM user_profile
UNION ALL
SELECT 'dish_features', COUNT(*) FROM dish_features
UNION ALL
SELECT 'recommendation_log', COUNT(*) FROM recommendation_log
UNION ALL
SELECT 'dish_similarity', COUNT(*) FROM dish_similarity;
```

### 测试存储过程
```sql
CALL sp_calculate_dish_popularity();
```

### 查看视图
```sql
SELECT * FROM v_user_behavior_stats;
SELECT * FROM v_dish_popularity;
```

## 下一步操作

### 1. 启动后端应用
```bash
cd JasEatsChoiceJava
mvn spring-boot:run
```

### 2. 测试API接口
```bash
# 获取推荐
curl http://localhost:8080/v1/recommend/recommend/1

# 记录用户行为
curl -X POST http://localhost:8080/v1/recommend/behavior \
  -H "Content-Type: application/json" \
  -d '{
    "userId": "1",
    "behaviorType": "click",
    "itemType": "dish",
    "itemId": "1"
  }'
```

### 3. 设置定时任务（可选）
```bash
# 编辑crontab
crontab -e

# 添加每天凌晨2点执行热度计算
0 2 * * * mysql -u root -p123456 jia_shi_yi_xuan -e "CALL sp_calculate_dish_popularity();"
```

## 注意事项

1. **MySQL保留关键字**：
   - `rank` 是保留关键字，已改用 `rank_position`
   
2. **JSON字段**：
   - 所有JSON字段使用MySQL的JSON类型
   - 查询时使用 `->` 或 `->>` 操作符
   
3. **字符集**：
   - 所有表使用 `utf8mb4` 字符集
   - 支持存储emoji和特殊字符

4. **索引优化**：
   - 已为高频查询字段创建索引
   - 使用DESC降序索引提高排序性能

## 迁移状态

✅ **数据库迁移成功完成**

所有推荐系统相关的表、视图、存储过程已成功创建并测试通过！
