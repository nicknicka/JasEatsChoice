# 🚀 聊天功能快速开始指南

## 📋 部署检查清单

### ✅ 第一步: 数据库迁移
```bash
# 1. 创建聊天表
mysql -u root -p123456 jia_shi_yi_xuan < create_complete_chat_tables.sql

# 2. 添加回复功能支持
mysql -u root -p123456 jia_shi_yi_xuan < add_message_reply_support.sql

# 3. 验证表创建
mysql -u root -p123456 -e "SHOW TABLES FROM jia_shi_yi_xuan LIKE 't_chat%';"
```

### ✅ 第二步: 重启后端服务
```bash
cd JasEatsChoiceJava
mvn clean package
java -jar target/JasEatsChoiceJava.jar
```

### ✅ 第三步: 启动前端
```bash
cd JasEatsChoiceFront
npm run build
npm run electron:serve
```

---

## 🎯 功能速查表

| 功能 | 快捷键/操作 | 说明 |
|------|-------------|------|
| **发送消息** | Enter | 发送消息 |
| **搜索消息** | 输入框输入关键词 | 实时搜索 |
| **转发消息** | 消息菜单 → 转发 | 跨会话转发 |
| **回复消息** | 消息菜单 → 回复 | 带引用回复 |
| **撤回消息** | 消息菜单 → 撤回 | 2分钟内有效 |
| **复制消息** | 消息菜单 → 复制 | 复制到剪贴板 |
| **导出记录** | 导出记录按钮 | 导出TXT文件 |
| **加载更多** | 滚动到顶部 | 自动加载 |
| **置顶会话** | 会话右键菜单 | 置顶会话 |

---

## 💡 使用技巧

### 1. 快速搜索历史消息
```
场景: 找到3天前讨论的"菜品推荐"
操作:
1. 打开对应会话
2. 在搜索框输入"菜品"
3. 点击搜索结果跳转
```

### 2. 批量转发重要信息
```
场景: 将订单信息转发到群聊
操作:
1. 找到订单消息
2. 点击"⋯" → "转发"
3. 选择目标群聊
4. 确认转发
```

### 3. 回复特定消息
```
场景: 回复朋友的具体问题
操作:
1. 找到要回复的消息
2. 点击"⋯" → "回复"
3. 看到回复预览后输入内容
4. 发送后自动显示引用
```

### 4. 导出聊天备份
```
场景: 保存与客户的对话记录
操作:
1. 打开会话
2. 点击"导出记录"
3. 自动下载TXT文件
```

### 5. 配置存储策略
```
场景: 保存30天的聊天记录
操作:
浏览器控制台执行:
localStorage.setItem('chatStorageDays', '30')
```

---

## 🔧 常见问题

### Q1: 消息发送失败怎么办?
**A**: 消息失败后会显示"发送失败"和"点击重发"按钮,直接点击即可重发。

### Q2: 搜索不到历史消息?
**A**: 确保已经加载了对应的消息记录,可以通过滚动到顶部加载更多。

### Q3: 撤回功能限制?
**A**: 只能撤回2分钟内发送的消息,且只能撤回自己的消息。

### Q4: 转发消息会显示来源吗?
**A**: 是的,转发消息会标记`forwardedFrom`字段。

### Q5: 导出的记录格式?
**A**: 导出为TXT格式,包含时间戳、发送者、内容和状态。

### Q6: 如何清除缓存?
**A**:
```javascript
// 清除聊天历史缓存
localStorage.removeItem('chatHistory')

// 重置存储配置
localStorage.removeItem('chatStorageDays')
localStorage.removeItem('chatMaxMessagesPerSession')
```

---

## 🎨 UI操作指南

### 消息操作菜单位置
```
┌─────────────────────────┐
│  消息内容              ⋯│  ← 鼠标悬停显示
└─────────────────────────┘
          ↓
    ┌─────────────┐
    │ 回复        │
    │ 转发        │
    │ 撤回消息    │
    │ 复制        │
    └─────────────┘
```

### 回复预览样式
```
┌─────────────────────────┐
│ 回复 @张三        [×] │  ← 点击×取消
│ 这是要回复的内容...    │
├─────────────────────────┤
│ 输入消息内容...        │
│               [发送]   │
└─────────────────────────┘
```

### 搜索结果面板
```
┌──────────────┐
│ 找到 3 条结果 [×]│
├──────────────┤
│ 10:30         │
│ 搜索关键词...  │
├──────────────┤
│ 09:15         │
│ 另一条消息...  │
└──────────────┘
```

---

## 📊 性能指标参考

| 指标 | 建议值 | 说明 |
|------|--------|------|
| 存储天数 | 7-30天 | 根据需求配置 |
| 每页消息数 | 50条 | 平衡性能与体验 |
| WebSocket重连 | 10次 | 最大重连次数 |
| 撤回时间限制 | 2分钟 | 用户期望值 |

---

## 🎓 最佳实践

### 1. 定期清理缓存
```javascript
// 建议每月执行一次
localStorage.clear()
```

### 2. 合理设置存储策略
```javascript
// 个人聊天: 7天
localStorage.setItem('chatStorageDays', '7')

// 重要群聊: 30天
localStorage.setItem('chatStorageDays', '30')
```

### 3. 导出重要对话
```javascript
// 重要业务对话建议及时导出备份
```

### 4. 使用搜索功能
```javascript
// 先搜索再加载更多,节省流量
```

---

## 🔗 相关文档

- [完整功能报告](聊天功能优化最终完成报告.md)
- [API接口文档](后端API文档.md)
- [数据库设计](导出的数据库结构.sql)

---

**祝使用愉快!** 🎉
